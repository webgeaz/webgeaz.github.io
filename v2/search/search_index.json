{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"S\u00d8AD developer guide","text":"<p>Build a complete web application using Jython controllers, SQL-backed models, and HTML views. This guide takes you from your first transaction to a protected project/task tracker deployed in a container.</p> <p>You need basic programming, HTML, and SQL knowledge. You do not need to write the Java engine or use AI to follow the tutorial. S\u00d8AD, SOAD, and soad refer to the same framework; S0ADM is its development and administration interface.</p>"},{"location":"#choose-your-path","title":"Choose your path","text":"Goal Start here Learn the framework 1. Architecture, then 2. Getting started Build the complete example 3. Planning through 9. Integrations Configure or replace login 8. Authentication Work in the browser IDE 11. S0ADM and AI Use a local editor or agent 12. CLI and agents Test and deploy 13. Testing, 14. Docker, 15. Kubernetes Publish application documentation 16. Publishing <p>Chapters have a learning sequence; the Reference navigation contains detailed contracts you can consult without interrupting the tutorial.</p>"},{"location":"#what-you-will-build","title":"What you will build","text":"<p>The tracker example includes projects, tasks, assignment to application users, status filtering, pagination, validation, private attachments, and a JSON endpoint. Members manage their own projects; an administrator permission controls destructive project deletion. Business ownership is checked independently of route permissions.</p> <p>The tutorial uses the <code>default</code> application and MySQL with local application authentication. Production deployment contains the application and authentication runtime, without the S0ADM IDE.</p>"},{"location":"#version-and-verification","title":"Version and verification","text":"<p>Run <code>soad version</code> and <code>soad COMMAND --help</code> when comparing instructions with another installation. Installed CLI releases may expose different optional flags. The example README records its verification checklist and limitations.</p> <p>No live account, API key, license, database password, or private registry is required merely to read this guide.</p>"},{"location":"chapters/01-architecture/","title":"1. Introduction and architecture","text":"<p>S\u00d8AD maps HTTP requests to small Jython classes called transactions. A transaction loads or changes database models and chooses a response: an HTML view, JSON, a download, or a redirect. Here, \u201ctransaction\u201d names the controller; the database transaction around a request is a separate concept.</p>"},{"location":"chapters/01-architecture/#the-components","title":"The components","text":"Component Responsibility Java engine Embedded Jetty server, configuration, JDBC pools, model generation, request context Jython runtime Transaction dispatch, rendering, authentication, filters, file uploads Application Your transactions, templates, public assets, schema, and security manifest S0ADM Browser development tools, application configuration, AI, and release preparation SOAD CLI Synchronize transaction source/views between a local workspace and S0ADM <pre><code>flowchart LR\n    Developer --&gt; IDE[S0ADM development server]\n    Developer --&gt; CLI[Local editor and SOAD CLI]\n    CLI --&gt; IDE\n    IDE --&gt; Release[Application release]\n    Release --&gt; Runtime[Application runtime]\n    Browser --&gt; Runtime\n    Runtime --&gt; DB[(Application database)]</code></pre> <p>The deployed runtime uses its own database accounts and application identities. It does not need the IDE database or an IDE session.</p>"},{"location":"chapters/01-architecture/#request-lifecycle","title":"Request lifecycle","text":"<pre><code>sequenceDiagram\n    participant B as Browser\n    participant F as Transaction filters\n    participant T as Transaction engine\n    participant A as Application auth\n    participant C as Controller\n    participant D as Database\n    B-&gt;&gt;F: HTTP request\n    F-&gt;&gt;T: Continue\n    T-&gt;&gt;D: Open request database transaction\n    T-&gt;&gt;A: Authorize action\n    A--&gt;&gt;T: Identity and access decision\n    T-&gt;&gt;C: Invoke allowed action\n    C-&gt;&gt;D: Query or update models\n    C--&gt;&gt;T: Renderer or redirect\n    T-&gt;&gt;D: Commit on success, rollback on exception\n    T--&gt;&gt;B: Response</code></pre> <p>Authorization failure prevents the controller action from running. Rendering is also part of request processing. External email/API calls and filesystem writes are not undone by a database rollback.</p>"},{"location":"chapters/01-architecture/#application-layout","title":"Application layout","text":"<pre><code>webapp/\n  assets/tracker/                  public CSS and other static assets\n  WEB-INF/\n    jython/default/\n      __init__.py\n      tracker/\n        __init__.py\n        project.py                class Project\n        _project/\n          project.html\n    security/app-security.json\n    deploy/auth-mysql.sql\n    deploy/auth-h2.sql\n    classes/models/               generated; do not author or commit\n</code></pre> <p><code>/t/tracker/project</code> invokes <code>Project.view(ctx)</code>. <code>/t/tracker/project/save</code> invokes <code>Project.save(ctx)</code>. The CLI uses an additional per-transaction directory locally; see the mapping.</p>"},{"location":"chapters/01-architecture/#language-and-database-boundaries","title":"Language and database boundaries","text":"<p>Transactions run on Jython 2.7, not Python 3. Use <code>\"%s\" % value</code> instead of f-strings. Java imports work directly; CPython binary packages and <code>pip install</code> are not an application dependency mechanism. Put reviewed Java dependencies in the application's runtime library directory and restart.</p> <p>ActiveJDBC models are generated from the live schema. Define tables first, then introspect or restart before importing new models. Business datasource support and built-in authentication support differ: the shipped local-auth SQL generator supports MySQL and H2, not every JDBC database.</p> <p>Next: start a development instance.</p>"},{"location":"chapters/02-getting-started/","title":"2. Getting started","text":"<p>By the end of this chapter you will have a working HTML transaction in a container-deployed S0ADM development environment.</p>"},{"location":"chapters/02-getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>The URL of a running S0ADM development deployment.</li> <li>An IDE account with developer access to the target application.</li> <li>A separate application account after authentication is configured.</li> <li>A browser. Install the <code>soad</code> CLI only when you want to use a local editor.</li> </ul> <p>Open the S0ADM URL supplied by your operator and sign in. Confirm that the target application and its datasource are available. Container startup, database credentials, volumes, and port mappings are deployment concerns; application developers do not need the S\u00d8AD framework source tree.</p> <p>For local-editor development, create an empty workspace and connect the CLI to that same IDE deployment:</p> <pre><code>mkdir hello-workspace\ncd hello-workspace\nsoad login --server https://ide.example.com\nsoad list\n</code></pre> <p>The CLI stores the selected server/application configuration in the workspace. Keep its credentials private. See CLI and agents for the full create, pull, compare, and push workflow.</p>"},{"location":"chapters/02-getting-started/#configure-the-default-application","title":"Configure the default application","text":"<p>Open System Settings \u2192 Applications \u2192 the default application. Configure security before expecting application routes to work. For the tracker choose:</p> Field Value Security profile authenticated Authentication mode Application-local identity Roles admin (first), member Registration invite_only <p>Save, follow the one-time setup link, and set an application password. The IDE password is not copied. Keep this account flow separate from the S0ADM login. See authentication for screenshots-free field instructions and the permission grants needed by the full tracker.</p>"},{"location":"chapters/02-getting-started/#create-your-first-transaction","title":"Create your first transaction","text":"<p>In the transaction editor create group <code>web</code>, code <code>hello</code>. Replace the controller:</p> <pre><code>from utils import render\n\nclass Hello(object):\n    ACCESS_POLICY = {\"view\": \"authenticated\"}\n\n    def view(self, ctx):\n        ctx.output[\"greeting\"] = \"Hello from S\u00d8AD\"\n        ctx.go_to = render.as_view(ctx, \"hello\")\n</code></pre> <p>Save the view as <code>_hello/hello.html</code> within that transaction:</p> <pre><code>&lt;!doctype html&gt;\n&lt;html lang=\"en\"&gt;\n&lt;head&gt;&lt;meta charset=\"utf-8\"&gt;&lt;title&gt;Hello&lt;/title&gt;&lt;/head&gt;\n&lt;body&gt;\n  &lt;h1&gt;{{greeting}}&lt;/h1&gt;\n  &lt;p&gt;Signed in as {{current_user.full_name}}&lt;/p&gt;\n  &lt;a href=\"{{logout_url}}\"&gt;Sign out&lt;/a&gt;\n&lt;/body&gt;\n&lt;/html&gt;\n</code></pre> <p>Save/compile and open <code>/t/web/hello</code> on your development deployment. Sign in with the application account if redirected. You should see \u201cHello from S\u00d8AD\u201d and your name. For a public brochure app, choose public profile/mode and an explicit public policy instead; do not change this protected tutorial to public to bypass setup.</p>"},{"location":"chapters/02-getting-started/#common-first-run-failures","title":"Common first-run failures","text":"Symptom Check IDE unavailable Deployment URL, container status, ingress/port, operator logs Missing models/table Correct application database; run Introspect after DDL 503 on application route Missing manifest, missing auth SQL, or mismatched checksum Redirect repeatedly to login Application account, cookie settings, HTTP vs HTTPS 404 after creating controller Group/code, capitalized class name, imports and logs <p>For a plain HTTP development deployment, the operator may set <code>session_cookie_secure=false</code> and <code>session_cookie_samesite=LAX</code>. Restore secure cookies for HTTPS deployment.</p> <p>Next: plan the tracker.</p>"},{"location":"chapters/03-planning/","title":"3. Plan a complete application","text":"<p>The tutorial is a project/task tracker for a small team. Begin with the access rules and database model, then build one working slice at a time.</p>"},{"location":"chapters/03-planning/#user-stories-and-access","title":"User stories and access","text":"<p>Members create projects and manage tasks inside projects they own. Each task has a title, a status, and an optional assignee selected from active application users. Assignment is informational: it does not grant access to the project. This keeps the first application's sharing model explicit and small.</p> <p>Administrators can delete their own projects, including their tasks and attachments. The <code>projects.delete</code> permission enables the route but does not bypass ownership. Runtime user administration is a separate <code>auth.users.manage</code> permission granted by <code>system_admin</code>.</p> Permission admin member projects.read yes yes projects.write yes yes projects.delete yes no tasks.read yes yes tasks.write yes yes <p>The source examples use exactly these codes. Configure them before opening the completed application. Avoid a \u201ccatch all\u201d public policy.</p>"},{"location":"chapters/03-planning/#data-model","title":"Data model","text":"<pre><code>erDiagram\n    SOAD_AUTH_USER ||--o{ PROJECT : owns\n    SOAD_AUTH_USER o|--o{ TASK : assigned\n    PROJECT ||--o{ TASK : contains\n    TASK ||--o| ATTACHMENT : holds\n    PROJECT {\n        int id PK\n        string owner_id FK\n        string name\n    }\n    TASK {\n        int id PK\n        int project_id FK\n        string title\n        string status\n        string assignee_id FK\n    }\n    ATTACHMENT {\n        int id PK\n        int task_id FK\n        blob content\n        string filename\n    }</code></pre> <p>Auth tables are generated by the framework. Business tables reference the local user ID but do not store passwords. Attachments are database blobs in this example so deletion and replacement participate in the same database transaction.</p>"},{"location":"chapters/03-planning/#delivery-milestones","title":"Delivery milestones","text":"<ol> <li>Configure local auth and confirm sign-in works.</li> <li>Install the schema, introspect, and create projects.</li> <li>Add editing, filtering, pagination, and permission checks.</li> <li>Add tasks, status changes, assignment, and attachments.</li> <li>Verify cross-user access denial and JSON output.</li> <li>Package the release, install production auth metadata, and deploy.</li> </ol> <p>For each milestone keep SQL and source together in version control. Record schema changes as ordered migrations; the CLI does not run them.</p> <p>The complete implementation is under examples/tracker. Build it manually with the next chapters or use the full source as a reference. AI is an optional development path described in chapter 11.</p>"},{"location":"chapters/04-database/","title":"4. Database and models","text":"<p>S\u00d8AD generates models from the connected database. Start with SQL, not handwritten model classes.</p>"},{"location":"chapters/04-database/#create-and-discover-tables","title":"Create and discover tables","text":"<p>Select the application's database in S0ADM's database explorer. Install local authentication first, then execute the tracker schema. The auth user table must exist before the business foreign keys can be created. Run Introspect if the operation has not already refreshed models. Restart after external schema changes if introspection is unavailable.</p> <p>The generated name capitalizes the table name: <code>project</code> becomes <code>Project</code>, <code>task</code> becomes <code>Task</code>, and <code>soad_auth_user</code> becomes <code>Soad_auth_user</code>. When a controller has the same name, alias the model:</p> <pre><code>from models import Project as ProjectModel\n\nclass Project(object):\n    pass\n</code></pre> <p>Generated classes and database connections belong to the runtime. Do not check generated models into Git or introduce a separate ORM session.</p>"},{"location":"chapters/04-database/#crud-operations","title":"CRUD operations","text":"<pre><code>from models import Project as ProjectModel\n\nproject = ProjectModel()\nproject.set(\"name\", \"Website launch\")\nproject.set(\"owner_id\", ctx.output[\"current_user\"][\"id\"])\nproject.saveIt()\n\nmine = ProjectModel.where(\"owner_id = ?\", ctx.output[\"current_user\"][\"id\"])\npage = mine.orderBy(\"id DESC\").limit(20).offset(0)\none = ProjectModel.first(\"id = ? AND owner_id = ?\",\n                         project.getId(), ctx.output[\"current_user\"][\"id\"])\none.set(\"name\", \"Website launch v2\")\none.saveIt()\n</code></pre> <p>Use <code>first(condition, values...)</code> for one match; do not call <code>.first()</code> on a LazyList. <code>findById(id)</code> works for a primary key, but private records should be loaded with an ownership condition too. Check for <code>None</code> before modifying a row. <code>delete()</code> deletes a loaded model; the tracker only exposes deletion through guarded POST actions.</p>"},{"location":"chapters/04-database/#inputs-search-and-relationships","title":"Inputs, search, and relationships","text":"<p>Bind values with <code>?</code> parameters. Do not interpolate submitted values into SQL. SQL identifiers and sort expressions cannot be value parameters; select them from a fixed allowlist instead.</p> <pre><code>sorts = {\"newest\": \"id DESC\", \"name\": \"name ASC, id ASC\"}\nsort = sorts.get(ctx.getParameter(\"sort\"), sorts[\"newest\"])\nrows = ProjectModel.where(\"owner_id = ? AND name LIKE ?\",\n                          ctx.output[\"current_user\"][\"id\"], \"%launch%\")\nrows = rows.orderBy(sort).limit(20)\n</code></pre> <p><code>LIKE</code> treats <code>%</code> and <code>_</code> as wildcards; decide whether your search UI permits these patterns. Limit page sizes and use stable ordering. The tracker fetches 21 rows and displays 20 to determine whether a next page exists.</p> <p>Use explicit foreign keys and indexes. When loading a task, authorize its parent project before reading or changing the task. A valid task ID does not prove the caller may access it.</p>"},{"location":"chapters/04-database/#ids-dates-and-blobs","title":"IDs, dates, and blobs","text":"<p>Use an auto-increment <code>id</code> for the tutorial and <code>saveIt()</code> for inserts/updates. If you deliberately use a manually assigned string/UUID primary key, use <code>insert()</code> for a new row and <code>saveIt()</code> for an existing row.</p> <p>Prefer explicit <code>created_date</code>/<code>updated_date</code> columns if you want application control over timestamps. <code>created_at</code> and <code>updated_at</code> have ActiveJDBC-managed semantics; do not set those reserved timestamps manually.</p> <p>The tracker has a separate attachment table to avoid loading blobs in list queries. Uploads are bounded and served through an authorized controller action; they are not public assets.</p>"},{"location":"chapters/04-database/#commit-rollback-and-migrations","title":"Commit, rollback, and migrations","text":"<p>The engine opens a database transaction around dispatch and rendering, commits on success, and rolls back on exceptions. Validate all input before writing. Returning a validation view or calling <code>sendError</code> is not, by itself, an instruction to undo earlier writes. Do not suppress an exception after partial updates and assume they rolled back.</p> <p>DDL may commit implicitly in MySQL. Keep migrations separate from web requests, back up first, apply each migration once, and regenerate models afterward. Reverting application source does not revert a database migration.</p> <p>For SQL bundles and older IDE schemas, see migrations.</p>"},{"location":"chapters/05-transactions/","title":"5. Transactions and routing","text":"<p>A transaction is a Jython class loaded by its group and code. For <code>tracker/project.py</code>, the class name is <code>Project</code>. For <code>address_book.py</code> it is <code>Address_book</code>, not <code>AddressBook</code>.</p>"},{"location":"chapters/05-transactions/#url-and-action-contract","title":"URL and action contract","text":"Request Controller action /t/tracker/project Project.view(ctx) /t/tracker/project/edit?id=7 Project.edit(ctx) /t/tracker/project/save Project.save(ctx) /other/t/tracker/project Other configured application's Project.view(ctx) <p>The normal default action is <code>view</code>. The request parameter <code>d_action</code> can select the action too. Give every public action an explicit policy, and prefix internal helpers with <code>_</code> so they cannot be invoked as routes.</p> <pre><code>class Example(object):\n    ACCESS_POLICY = {\"view\": \"authenticated\", \"save\": \"authenticated\"}\n\n    def view(self, ctx):\n        ctx.go_to = \"/t/web/hello\"\n\n    def save(self, ctx):\n        \"\"\"POST\"\"\"\n        # Validate first, then save and redirect.\n        ctx.go_to = ctx.ctxPath + \"/t/web/hello\"\n\n    def _normalize(self, value):\n        return (value or \"\").strip()\n</code></pre> <p>For mutation actions, the docstring must start exactly with <code>POST</code>, <code>PUT</code>, or <code>DELETE</code>. The engine checks these prefixes for explicitly selected actions. A <code>GET</code> docstring is not a general GET-only guard; keep <code>view</code> read-only. Use POST forms for tutorial writes and deletions.</p>"},{"location":"chapters/05-transactions/#request-and-response-context","title":"Request and response context","text":"API Use ctx.getRequest(), ctx.getResponse() Jakarta servlet request/response ctx.getParameter(\"name\") Ordinary submitted parameter ctx.getHeader(\"HX-Request\") Request header ctx.json() Parse JSON body to a map ctx.output Values available to the view ctx.output[\"current_user\"] Authorized application identity ctx.ctxPath Framework routing prefix; preserve it in links ctx.go_to Redirect string or response callable ctx.page_layout = False Disable wrapper for fragments/JSON/downloads <p><code>render.as_view(ctx, \"edit\")</code> returns a callable; assign it to <code>ctx.go_to</code>. Returning a value from an action is not the normal response mechanism. <code>ctx.redirect(url)</code> sets the redirect destination.</p>"},{"location":"chapters/05-transactions/#validation-and-response-status","title":"Validation and response status","text":"<p>Trim text, enforce lengths, allowlist enumerations, and parse IDs before database access. On invalid input re-render the form with the submitted values and field feedback. HTML validation is convenience; the controller enforces the rules.</p> <p><code>as_view</code> sets status 200 when invoked. To return an HTML form with 400:</p> <pre><code>from utils import render\n\ndef _invalid_form(ctx):\n    render_view = render.as_view(ctx, \"edit\")\n    def response():\n        html = render_view()\n        ctx.getResponse().setStatus(400)\n        return html\n    ctx.go_to = response\n</code></pre> <p>For missing private records, return 404 without revealing who owns them. For invalid parameters use 400. A denied permission or invalid CSRF token gives 403; missing security setup gives 503. Always inspect response content and redirects when testing, not just whether a request completed.</p> <p>Next: render forms and layouts.</p>"},{"location":"chapters/06-views/","title":"6. Views and frontend development","text":"<p>Views are Handlebars HTML templates. A transaction named <code>tracker/project</code> loads its views from <code>tracker/_project/</code> at runtime.</p>"},{"location":"chapters/06-views/#template-data","title":"Template data","text":"<pre><code>ctx.output[\"heading\"] = \"My projects\"\nctx.output[\"projects\"] = projects\nctx.go_to = render.as_view(ctx, \"project\")\n</code></pre> <pre><code>&lt;h1&gt;{{heading}}&lt;/h1&gt;\n{{#each projects}}\n  &lt;a href=\"{{../ctxPath}}/t/tracker/project/edit?id={{id}}\"&gt;{{name}}&lt;/a&gt;\n{{else}}\n  &lt;p&gt;No projects yet. Create your first project.&lt;/p&gt;\n{{/each}}\n</code></pre> <p><code>{{value}}</code> escapes HTML. Only mark framework-generated layout content or explicitly trusted HTML as safe; never render a user's name, description, or error with triple braces. Within <code>each</code>, use <code>../</code> to reach parent context.</p> <p>The renderer supplies comparison helpers, <code>select</code>, <code>option</code>, <code>ref_lookup</code>, <code>dateFmt</code>, <code>length</code>, <code>size</code>, <code>html</code>, <code>session</code>, <code>get</code>, and <code>i18n</code>. See the rendering reference for signatures and limits.</p>"},{"location":"chapters/06-views/#shared-page-layout","title":"Shared page layout","text":"<p>A base transaction can provide a layout location that page transactions inherit:</p> <pre><code>class Layout(object):\n    def page_layout(self, ctx):\n        return (\"tracker\", \"layout\", \"layout\")\n\nclass Project(Layout):\n    pass\n</code></pre> <p>The engine extracts the rendered document's body/head/title and combines them with the layout. The tracker layout uses <code>{{body}}</code> for the framework-supplied safe content, while normal template values remain escaped. The layout controller must implement <code>view(ctx)</code>, even when it only contains <code>pass</code>; the engine invokes it to prepare layout data. The IDE policy audit requires both public methods on the base layout to be covered.</p> <p>Use a complete document for pages; set <code>ctx.page_layout = False</code> for a fragment, JSON response, or download. Do not wrap JSON inside the site's HTML shell.</p>"},{"location":"chapters/06-views/#forms-and-navigation","title":"Forms and navigation","text":"<pre><code>&lt;form method=\"post\" action=\"{{ctxPath}}/t/tracker/project/save\"&gt;\n  &lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{csrf_token}}\"&gt;\n  &lt;input type=\"hidden\" name=\"id\" value=\"{{project.id}}\"&gt;\n  &lt;label for=\"name\"&gt;Project name&lt;/label&gt;\n  &lt;input id=\"name\" name=\"name\" value=\"{{project.name}}\" maxlength=\"120\" required&gt;\n  {{#if error}}&lt;p role=\"alert\"&gt;{{error}}&lt;/p&gt;{{/if}}\n  &lt;button type=\"submit\"&gt;Save project&lt;/button&gt;\n&lt;/form&gt;\n</code></pre> <p>Successful POST actions redirect to a GET page, avoiding duplicate writes on refresh. Include visible labels, keyboard focus, readable contrast, a viewport meta tag, and useful empty states. Use a horizontally scrollable wrapper for tables on small screens.</p>"},{"location":"chapters/06-views/#tailwind-and-assets","title":"Tailwind and assets","text":"<p>The tracker includes a Tailwind input stylesheet and a small dependency manifest. Build its CSS before uploading <code>webapp/assets/tracker/tracker.css</code>; see the example setup. Serve compiled assets locally, without the Tailwind browser CDN in production.</p> <p>Your application owns its assets. S0ADM's component classes and asset routes are IDE implementation details and are absent from the production application image. When extending S0ADM itself, follow its existing Tailwind component layer and rebuild its packaged stylesheet.</p>"},{"location":"chapters/06-views/#progressive-enhancement","title":"Progressive enhancement","text":"<p>Ordinary forms work without JavaScript. To add htmx, package its reviewed script in your application assets, then attach attributes:</p> <pre><code>&lt;form hx-post=\"{{ctxPath}}/t/tracker/project/save\"\n      hx-target=\"#project-panel\" method=\"post\"\n      action=\"{{ctxPath}}/t/tracker/project/save\"&gt;\n  &lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{csrf_token}}\"&gt;\n  &lt;!-- The same labeled inputs as the ordinary form. --&gt;\n&lt;/form&gt;\n</code></pre> <p>Provide a controller response appropriate for both modes: an HTML fragment with layout disabled for htmx, or the normal redirect for a full form submission. The downloadable tracker uses ordinary forms throughout, so enhancement is optional rather than a prerequisite.</p>"},{"location":"chapters/07-building-the-tracker/","title":"7. Build the project/task tracker","text":"<p>This chapter connects the database, transaction, and view concepts into one application. The complete source is included so you can compare your work at each stage.</p>"},{"location":"chapters/07-building-the-tracker/#1-prepare-identity-and-data","title":"1. Prepare identity and data","text":"<p>Complete the setup, then configure the five permissions and grants in the plan. Install schema.sql in the application's database and run Introspect. The <code>Project</code>, <code>Task</code>, <code>Attachment</code>, and <code>Soad_auth_user</code> models must be available before the controllers import them.</p> <p>Create transactions in this order:</p> Group/code Controller Views tracker/layout Layout layout.html tracker/project Project project.html, edit.html tracker/task Task task.html, edit.html <p>Copy their contents from the example's <code>webapp/WEB-INF/jython/default/tracker/</code> directory. For the CLI use <code>soad create</code> and then place source in the nested CLI layout described in chapter 12.</p>"},{"location":"chapters/07-building-the-tracker/#2-create-and-list-projects","title":"2. Create and list projects","text":"<p><code>Project.view</code> reads only rows whose <code>owner_id</code> equals <code>ctx.output[\"current_user\"][\"id\"]</code>. Its query binds the search value and limits results to 20 plus one look-ahead row.</p> <p><code>Project.save</code> trims and checks the name before writing. A new project's owner is taken from the authenticated context, never from a hidden form field. An existing project is loaded with both its ID and owner ID.</p> <p>Open <code>/t/tracker/project</code>, create \u201cWebsite launch\u201d, and confirm it appears. Edit its name. Submit an empty name with browser validation bypassed and verify the controller returns feedback without creating a row.</p>"},{"location":"chapters/07-building-the-tracker/#3-add-tasks-and-assignment","title":"3. Add tasks and assignment","text":"<p><code>Task</code> imports the project's private ownership helpers. Every task action loads the requested project through that ownership check before loading a task. A task is then looked up by both <code>id</code> and <code>project_id</code>, preventing a caller from combining their project ID with somebody else's task ID.</p> <p>Create \u201cWrite homepage\u201d, assign an active application user, and change its status through <code>todo</code>, <code>doing</code>, and <code>done</code>. The server rejects any other status and rejects nonexistent or disabled assignees. The assignee selector passes only ID and display name to the view, not entire authentication records.</p> <p>Invite a second application user through <code>/_soad/auth/admin/users</code> to populate the selector. Assignment is a label, not project membership.</p>"},{"location":"chapters/07-building-the-tracker/#4-add-presentation-and-navigation","title":"4. Add presentation and navigation","text":"<p>Build and upload the example's CSS as described in its README. The layout provides navigation, profile/sign-out links, a skip link, and a content area. Forms preserve submitted values on errors. Tables scroll on narrow screens.</p> <p>Set the default application's Welcome transaction to <code>tracker/project</code> in System Settings. Its persisted release setting is <code>WEB-INF/sufia-app.properties</code>; an operator's <code>sufia_welcome_transaction</code> environment override takes precedence.</p>"},{"location":"chapters/07-building-the-tracker/#5-add-attachments-and-json","title":"5. Add attachments and JSON","text":"<p>Upload a small text file while saving a task. Download it through the task's authorized download action. Removing or replacing an attachment happens within the database transaction. The example deliberately serves bytes as an attachment, with <code>application/octet-stream</code> and <code>nosniff</code>.</p> <p>Visit:</p> <pre><code>/t/tracker/task/data?project_id=PROJECT_ID\n</code></pre> <p>You should receive a JSON object containing a bounded task list, page number, and <code>has_next</code>. This endpoint uses the same login and ownership rules as HTML; it does not automatically become a bearer-token API.</p>"},{"location":"chapters/07-building-the-tracker/#6-prove-the-access-rules","title":"6. Prove the access rules","text":"<p>Use two separate browser sessions. Create a project as user A; sign in as member B and request A's project edit URL, task URLs, and attachment URL. Each must deny access without returning A's data.</p> <p>As a member, POSTing project deletion should return 403 even for your own project. As an administrator, deletion of your own project succeeds and cascades to tasks/attachments. An administrator still cannot delete somebody else's project through this tutorial's routes.</p> <p>The delete controls disclose the action and consequence before the final submit. The server policy is authoritative even when a control is visible.</p>"},{"location":"chapters/07-building-the-tracker/#completion","title":"Completion","text":"<p>You now have a complete CRUD application with separate accounts, data ownership, roles, assignment, filtering, pagination, uploads, and JSON output. Run the acceptance checks before following the deployment chapters. Team sharing, notifications, and optimistic concurrency are deliberate extension exercises, not hidden requirements of the supplied implementation.</p>"},{"location":"chapters/08-authentication/","title":"8. Built-in authentication: use, configure, and override","text":"<p>S\u00d8AD supplies application-local login and authorization. The application's users, roles, permissions, tokens, and revocable sessions live in its own datasource. An S0ADM login does not authenticate a user to your web application.</p>"},{"location":"chapters/08-authentication/#choose-a-mode","title":"Choose a mode","text":"Profile / mode Use authenticated / local Protected application using built-in accounts mixed / local Public pages plus protected actions public / public Entirely public application, without a login provider authenticated or mixed / custom Your provider owns identity and authentication <p>The security manifest at <code>WEB-INF/security/app-security.json</code> defines the application's mode, roles, permission grants, and registration policy. Changing a controller policy does not register its permission automatically.</p> <pre><code>flowchart TD\n    Request --&gt; Manifest{Security metadata ready?}\n    Manifest --&gt;|No| Unavailable[503]\n    Manifest --&gt;|Yes| Policy{Action policy}\n    Policy --&gt;|Missing| Denied[403]\n    Policy --&gt;|Public| Action[Run action]\n    Policy --&gt;|Protected| Identity{Signed in?}\n    Identity --&gt;|No| Login[Login redirect]\n    Identity --&gt;|Yes| Checks[Check CSRF for mutation and permission]\n    Checks --&gt;|Denied| Denied\n    Checks --&gt;|Allowed| Owner[Application ownership validation]\n    Owner --&gt; Action</code></pre>"},{"location":"chapters/08-authentication/#set-up-the-built-in-provider","title":"Set up the built-in provider","text":"<ol> <li>Open System Settings \u2192 Applications \u2192 your application as an IDE superadmin.</li> <li>Initialize the Security card with <code>authenticated</code>, local identity, and roles    <code>admin</code> and <code>member</code>. Save and follow the one-time application-password link.</li> <li>Open that application's Roles and permissions section. Add the codes from    the tracker plan and select their    grants. Save to publish the manifest and install matching development metadata.</li> <li>Keep registration <code>invite_only</code> for this tutorial. Open registration requires    a least-privilege <code>member</code> role and must not grant administrator access.</li> <li>Test an application route with the new application account.</li> </ol> <p>Initial role setup and later permission editing are different forms. The platform-level Roles screen controls IDE access; it is not the application's permission editor. An unregistered <code>permission:code</code> denies every user; inspect the application's policy audit and logs if even an administrator receives 403.</p>"},{"location":"chapters/08-authentication/#protect-controllers-and-records","title":"Protect controllers and records","text":"<pre><code>class Project(object):\n    ACCESS_POLICY = {\n        \"view\": \"permission:projects.read\",\n        \"save\": \"permission:projects.write\",\n        \"delete\": \"permission:projects.delete\",\n    }\n</code></pre> <p>Use <code>public</code>, <code>authenticated</code>, or <code>permission:&lt;registered-code&gt;</code> for each action. Set POST/PUT/DELETE method docstrings on mutations. Authenticated local mutations accept the <code>csrf_token</code> form field or <code>CSRF-Token</code> request header. For JSON, put the token in the header, not only inside the JSON body.</p> <p>Public local-auth actions receive a token in the template context, but the business action must validate CSRF itself for public state changes. The runtime's protected-action check does not make public writes safe automatically.</p> <p>Policies decide whether a caller may run an action. Your query must still enforce record ownership, organization membership, or other business access rules.</p>"},{"location":"chapters/08-authentication/#built-in-pages-and-settings","title":"Built-in pages and settings","text":"Route under /_soad/auth Purpose /login, /logout Sign in and sign out /bootstrap First administrator setup, gated by absence of an active admin /register Self-registration when enabled /forgot, /reset Password recovery /invite Invitation and initial password setup /profile, /password Profile/password management /totp/setup TOTP enrollment/removal /sessions List/revoke sessions /admin/users Account creation, invitations, status, roles /health Manifest/schema readiness <p>Create the first production admin with <code>soad-auth-admin</code> before exposing the service, or complete the bootstrap page on a restricted setup connection. Browser bootstrap grants <code>system_admin</code>; use its user administration page to grant the business <code>admin</code> role afterward. The administrative CLI also grants the manifest's generated owner/admin role. Use invitation setup links as secrets. Password resets and account disabling revoke active sessions. TOTP is implemented; do not describe it as future work.</p> <p>Configure <code>sufia_hostname</code> as the public HTTPS origin and <code>sufia_app_name</code> as the display name. SMTP uses <code>mail_smtp_host</code>, <code>mail_smtp_port</code>, <code>mail_smtp_username</code>, <code>mail_smtp_password</code>, and <code>mail_sender_email</code>. See the mail transport limitation in integrations.</p> <p>The checked-in production profile has <code>session.cookie.secure=true</code>, <code>session.cookie.httponly=true</code>, <code>session.cookie.samesite=NONE</code>, and a 1,800-second idle timeout. This guide's same-site tracker deployments explicitly choose <code>LAX</code>; keep <code>NONE</code> only when your cross-site flow needs it and always use HTTPS. The local auth session record also has its own expiry; changing the servlet idle timeout does not change that database session lifetime.</p>"},{"location":"chapters/08-authentication/#override-appearance-without-replacing-authentication","title":"Override appearance without replacing authentication","text":"<p>Put validated templates under <code>WEB-INF/auth-overrides/</code>; shared CSS belongs at <code>assets/auth-overrides/auth.css</code>. Start with <code>login.html</code>, and retain its field names, action URLs, CSRF field, and required page structure.</p> <p>A template is not an ordinary application view: the override validator permits only its supported template/markup contract. Do not reuse the tracker layout or load a CDN. Use the full screen-by-screen contract for the exact required inputs and context for every screen, including bootstrap.</p> <p>Override <code>mark.svg</code> to replace the small-screen logo. Missing or invalid templates/styles/marks fall back to the bundled UI and log the reason. Removing an override restores the default. Presentation overrides do not change password, session, or permission verification.</p>"},{"location":"chapters/08-authentication/#replace-authentication-behavior","title":"Replace authentication behavior","text":"<p>Choose Custom application provider and a lowercase module such as <code>security/auth_provider</code>. Its <code>Auth_provider</code> class must implement:</p> Method Contract current_user(ctx) Current identity or None authenticate(ctx) Authenticated identity or None authorize(ctx, action, permission) Exactly True to grant the permission validate_csrf(ctx) Exactly True to accept a protected mutation login_url(ctx), logout_url(ctx) URLs for your authentication flow <p>The provider module is not directly routable. Put login/callback actions in separate transactions with explicit policies. A custom provider must supply credential verification or a complete external identity integration, session handling, revocation, callback/return validation, and CSRF behavior. Selecting <code>custom</code> does not install OIDC or SSO automatically.</p> <p>See the provider skeleton and fail-closed rules. The skeleton deliberately grants nothing until implemented. After changing modes, publish matching metadata/SQL and test before deploying. Keep old local tables during an initial rollback window; the runtime does not drop them automatically.</p> <p>Transaction-owned auth pages are an advanced local-provider integration, distinct from templates and custom providers. Their route mapping must be registered in the manifest; do not implement a second password store or assume the archived roadmap is a current API contract.</p>"},{"location":"chapters/09-integrations/","title":"9. Files, APIs, and integrations","text":"<p>Add integrations after the main CRUD flow and its access rules work.</p>"},{"location":"chapters/09-integrations/#upload-and-download","title":"Upload and download","text":"<p>Use <code>enctype=\"multipart/form-data\"</code> and a named file input. The framework's request wrapper exposes an <code>UploadedFile</code> through <code>getParameter</code>:</p> <pre><code>upload = ctx.getRequest().getParameter(\"attachment\")\nif upload:\n    filename = upload.getFileName()\n    size = upload.getSize()\n    content = upload.getBytes()       # only for a deliberately bounded small file\n</code></pre> <p>It also supports <code>getInputStream()</code> and <code>writeTo(destination)</code>. The wrapper must be accessed through <code>ctx.getRequest()</code> for files: the convenience <code>ctx.getParameter()</code> has a Java String return type and cannot return UploadedFile. The wrapper provides <code>&lt;field&gt;_fn</code> and <code>&lt;field&gt;_ft</code> metadata. Empty uploads are falsey. Do not call multipart parsing again inside the controller. Temporary files are cleaned when request processing finishes; copy or consume them during the action.</p> <p>Configure <code>max.file.upload.size</code>, <code>max.request.upload.size</code>, and optionally <code>file.upload.temp.dir</code>. Oversized requests return 413. The tracker adds a 1 MiB application limit and sanitizes the download filename; it never uses the submitted name as a filesystem path.</p> <p>The tracker reads an attachment only after checking project ownership and the task's project membership. It sets <code>attachment=True</code>, an opaque content type, and <code>X-Content-Type-Options: nosniff</code>. It does not scan malware or make uploaded content safe to execute. Add scanning/quarantine if your application's use requires it.</p> <p>For large files use bounded streaming and private storage with generated keys. If a filesystem write accompanies a database update, define cleanup/retry behavior explicitly: database rollback does not remove the file.</p>"},{"location":"chapters/09-integrations/#json-and-ajax","title":"JSON and AJAX","text":"<pre><code>ctx.page_layout = False\nctx.go_to = render.as_json(ctx, {\"ok\": True, \"project_id\": project.getId()})\n</code></pre> <p>Pass a deliberately selected object. Calling <code>as_json(ctx)</code> without one serializes all output, which may include identity and CSRF context unsuitable for the endpoint. Never serialize whole auth models.</p> <p>Use <code>ctx.json()</code> for request JSON and validate keys/types before updates. Cookie-authenticated AJAX must carry cookies and the <code>CSRF-Token</code> header on protected writes. A login redirect is not a JSON success response; clients should detect expired authentication and navigate to sign-in.</p> <p>The framework does not infer CORS, bearer authentication, or an API versioning scheme from a JSON response. Design those requirements explicitly with filters and/or a custom provider.</p>"},{"location":"chapters/09-integrations/#email","title":"Email","text":"<p>The bundled mail utility is <code>from utils import mailer</code>. Its concrete entrypoints are listed in rendering and utilities. Set mail configuration through environment variables and restart after changes; the utility establishes configuration at module load.</p> <p>Use a test relay before enabling account recovery in production. Confirm sender identity, public HTTPS links, and delivery with your mail operator. The current bundled SMTP helper explicitly trusts all TLS certificates on its STARTTLS path. It should not be described as certificate-verified transport; use a controlled relay/network boundary or remediate the mail transport before relying on it across an untrusted network.</p> <p>Do not send email inside a request and assume database rollback can recall it. For reliable notifications, persist an outbox entry in the transaction and have an external worker deliver/retry it. S\u00d8AD does not automatically provision that worker.</p>"},{"location":"chapters/09-integrations/#reports-and-external-services","title":"Reports and external services","text":"<p><code>render.as_pdf</code> renders a Handlebars view through the bundled PDF converter. Use a report-specific, simple HTML/CSS template rather than expecting browser JavaScript or a full Tailwind layout to print identically.</p> <p>Apache POI can generate Excel workbooks. Use server-generated filenames and the file/blob renderers for downloads. Escape spreadsheet formula-leading user text when exporting untrusted strings.</p> <p>Java's HTTP client can call external services from Jython. Set connection/request timeouts, validate response status, keep keys in environment-backed configuration, and decide whether failure should abort the business update or be retried later. CPython libraries such as <code>requests</code> are not part of this runtime.</p>"},{"location":"chapters/10-filters-errors/","title":"10. Filters, errors, and logging","text":"<p>Use filters for request-wide behavior, controller policies for action access, and business queries for record ownership.</p>"},{"location":"chapters/10-filters-errors/#transaction-filters","title":"Transaction filters","text":"<p>Create a transaction under the reserved <code>default/filter/</code> group with the capitalized class and all three lifecycle methods:</p> <pre><code>class Headers(object):\n    url_mapping = \"/t/*\"\n\n    def filter_init(self, config):\n        pass\n\n    def do_filter(self, request, response, chain):\n        response.setHeader(\"X-Content-Type-Options\", \"nosniff\")\n        chain.doFilter(request, response)\n\n    def destroy(self):\n        pass\n</code></pre> <p>Call the chain exactly once to continue; omit it only when completing the response yourself. Filters are shared across requests, so do not store a user, request, or database connection on <code>self</code>.</p> <pre><code>flowchart TD\n    Request --&gt; FilterA[First filter before]\n    FilterA --&gt; FilterB[Second filter before]\n    FilterB --&gt; Servlet\n    Servlet --&gt; ReturnB[Second filter after]\n    ReturnB --&gt; ReturnA[First filter after]\n    ReturnA --&gt; Response</code></pre> <p>Discovery/instances are cached: restart after adding, removing, renaming, or editing a filter. The default-app filter group is the discovery source, not one independent group per datasource. See the filter reference for exact ordering, wildcard mappings, scope exclusions, and failure behavior.</p>"},{"location":"chapters/10-filters-errors/#error-pages","title":"Error pages","text":"<p>The framework has separate defaults for missing resources (404), explicit 500 errors, and uncaught exceptions. Optional default-app transactions are:</p> Code Template errors/err404 errors/_err404/err404.html errors/err500 errors/_err500/err500.html errors/errexc errors/_errexc/errexc.html <p>Error override <code>view(ctx)</code> runs in a special lifecycle: no normal auth, application filter, database transaction, or page layout. Keep it independent of the database so it works during outages. IDE and internal routes use bundled pages rather than business overrides.</p> <p>The original status is retained, even when a renderer sets 200. An invalid override falls back to the bundled error page. <code>ctx.getError()</code> exposes details only during error dispatch; those details are not automatically displayed. See the full error contract.</p>"},{"location":"chapters/10-filters-errors/#logs-and-diagnosis","title":"Logs and diagnosis","text":"<pre><code>from sufia.util import Log\nLog.info(ctx, \"Project save completed\")\n</code></pre> <p>Use the IDE log viewer in development and container stdout/stderr for deployed applications. Do not log passwords, setup/reset links, session cookies, provider keys, or uploaded contents.</p> <p>The development mode can log request and session information and reloads transaction modules. Use <code>sufia.mode=production</code> in production. An unexplained 404 may be an import failure; inspect the accompanying log before changing routing. A 503 on protected routes often means authentication schema/manifest mismatch rather than a controller exception.</p>"},{"location":"chapters/11-s0adm/","title":"11. S0ADM features, including AI","text":"<p>S0ADM is the browser development environment for S\u00d8AD. Use it to create and test applications, manage development metadata, and prepare releases. Keep it on a restricted development/control-plane instance.</p>"},{"location":"chapters/11-s0adm/#workspace-and-application-tools","title":"Workspace and application tools","text":"Feature Developer workflow Applications/settings Select/configure apps, developer access, security, welcome route Transaction editor Create group/code, edit Jython and views, save/compile, preview Editor workspace Open multiple transactions/files, switch tabs, retain drafts Versions Inspect prior transaction saves and restore a selected version File manager Browse folders, create/upload/rename/edit/download application files Database explorer Inspect schema, tables/columns, records, DDL, query results SQL editor Execute reviewed SQL, inspect history/results, refresh generated models Logs Diagnose compile/runtime/database errors People and profile IDE users/roles, access, profile, sessions, and account security Gallery Discover/import packaged application content where configured Release/deployment Prepare an application image and view build progress <p>Saving transaction source is not the same as applying database DDL. When adding a column, update the schema, refresh models, then save the controller and template. Unsaved editor drafts are not deployed source; check the saved version before export or release.</p> <p>System settings and deployment actions require elevated IDE privileges. Features also depend on the image, license, datasource, and installed tools. The deployment screen reports unavailable builds when Kaniko is absent; a standalone image does not imply in-cluster builds are enabled.</p>"},{"location":"chapters/11-s0adm/#configure-the-ai-assistant","title":"Configure the AI assistant","text":"<p>As superadmin, open System Settings \u2192 Applications \u2192 an application \u2192 AI assistant. Configure provider family, endpoint, API key, model, and enabled state. These settings are platform-wide, even when reached from one application's page.</p> <p>Supported wire families are OpenAI-compatible chat completions, OpenAI Responses, and Anthropic messages. Use a model available from your configured provider; the guide does not pin a changing provider model catalog.</p> Property Environment variable ai.enabled ai_enabled ai.provider ai_provider ai.endpoint ai_endpoint ai.api.key ai_api_key ai.model ai_model ai.auto.accept ai_auto_accept <p>Resolution is per field: saved IDE settings, then environment, properties, and built-in default. Auto-accept defaults off. Restart for property/environment changes. Keep secrets in managed configuration rather than pasted application code.</p>"},{"location":"chapters/11-s0adm/#understand-the-two-ai-workflows","title":"Understand the two AI workflows","text":"<p>Transaction editor chat: open a transaction and choose AI/Edit with AI. Ask it to read code, diagnose logs, propose changes, or restore versions. Ordinary source edits are staged for diff review; Approve &amp; Execute publishes them. Auto-accept can automate eligible approvals when enabled.</p> <p>Dashboard whole-app generation: describe a complete app on a suitable empty application. This has a separate server-authorized workflow: it can execute SQL, install provisional transactions, review and validate them, and publish the result. Do not assume that no database or source changes occur before final publication. Follow the visible workflow state and use a disposable development database for a first attempt.</p> <pre><code>flowchart TD\n    Prompt --&gt; Mode{Development workflow}\n    Mode --&gt;|Editor chat| Read[Read source and schema]\n    Read --&gt; Stage[Stage changes and show diff]\n    Stage --&gt; Approval[Manual approval or enabled auto-accept]\n    Approval --&gt; Save[Save changes]\n    Mode --&gt;|Whole-app generation| Plan[Prepare app and security plan]\n    Plan --&gt; Build[Authorized schema and provisional source changes]\n    Build --&gt; Validate[Review and runtime validation]\n    Validate --&gt; Publish[Publish or report failure]</code></pre> <p>Normal chat SQL blocks have an Execute action for the developer. Rename/move is an immediate tool operation with conversational confirmation semantics; check internal links afterward. These are different from staged file editing. See AI workflows for details.</p>"},{"location":"chapters/11-s0adm/#a-useful-tracker-prompt","title":"A useful tracker prompt","text":"<p>Build an authenticated project/task tracker using local application identity. Members own projects and may manage tasks in their own projects. Assignment does not grant access. Register projects.read, projects.write, tasks.read, tasks.write for admin/member, and projects.delete for admin. Check ownership on HTML, JSON, and download actions. Use Jython 2.7, Handlebars, and local compiled Tailwind assets. Show the schema and review the access rules.</p> <p>For larger work, use the planning guide to create a schema, route/action list, acceptance criteria, and phases before generation. Project <code>AGENTS.md</code> conventions help preserve naming and design choices.</p> <p>Review generated SQL, permissions, uploads, and business validations just as you would review manually written code. Model output and runtime validation are not a substitute for testing with different user accounts.</p>"},{"location":"chapters/12-cli-agents/","title":"12. SOAD CLI and local agent workflows","text":"<p>The SOAD CLI lets you use a local editor while synchronizing transactions with an S0ADM server. It does not run the Java server, execute schema migrations, or turn a source sync into a complete production release.</p>"},{"location":"chapters/12-cli-agents/#install-and-authenticate","title":"Install and authenticate","text":"<p>Obtain the CLI from your S\u00d8AD distribution. The checked installer targets Webgeaz-Sdn-Bhd releases; older documentation refers to webgeaz/soadcli. If release access is restricted, use the installer supplied with your S\u00d8AD installation.</p> <p>The installer expects archives named <code>soadcli_&lt;os&gt;_&lt;arch&gt;.tar.gz</code>, containing a <code>soad</code> executable. macOS uses <code>darwin</code>, Linux uses <code>linux</code>, Apple Silicon uses <code>arm64</code>, and x86-64 uses <code>amd64</code>. After downloading and extracting the matching archive, a user-local installation is:</p> <pre><code>mkdir -p \"$HOME/.local/bin\"\ninstall -m 755 /path/to/extracted/soad \"$HOME/.local/bin/soad\"\nexport PATH=\"$HOME/.local/bin:$PATH\"\n</code></pre> <p>Add that PATH entry to your shell startup configuration to retain it. The shipped shell installer instead installs into /usr/local/bin using sudo; inspect it before running it. Native Windows packaging is not verified by this guide; use a supported Linux environment such as WSL with the appropriate binary.</p> <pre><code>soad version\nsoad --help\nmkdir tracker-workspace\ncd tracker-workspace\nsoad login --server https://ide.example.com\nsoad list\n</code></pre> <p>Interactive login saves project configuration and credentials in <code>.config.json</code>. Keep it out of source-control systems, release archives, and agent prompts. Use separate project directories for development and production connections. For noninteractive login, the verified CLI supports <code>SOAD_LOGIN</code> and <code>SOAD_PASSWORD</code>, or <code>SOAD_API_KEY</code> and <code>SOAD_APP_ID</code>; inject them from your secret manager instead of command arguments.</p> <p>Use <code>soad COMMAND --help</code> to confirm the flags supported by your installed version.</p>"},{"location":"chapters/12-cli-agents/#local-and-runtime-paths","title":"Local and runtime paths","text":"File CLI workspace Runtime under WEB-INF/jython/default Controller src/tracker/project/project.py tracker/project.py View src/tracker/project/_project/edit.html tracker/_project/edit.html <p>Do not copy the extra per-transaction CLI directory directly into runtime routing. The CLI's checksum file tracks local changes, but is not a database migration ledger or a source-control history.</p>"},{"location":"chapters/12-cli-agents/#daily-development","title":"Daily development","text":"<pre><code>soad create tracker/project\n# Edit the scaffolded controller and views in your editor.\nsoad changed\nsoad compare tracker/project\nsoad push tracker/project\n</code></pre> <p>For an existing transaction, pull before starting work:</p> <pre><code>soad pull tracker/project\n</code></pre> <p>Preserve any unpushed local edits before pulling. Coordinate with other developers and compare against the current server before pushing; do not assume automatic semantic merging.</p> Command Use pull --all Download all transactions push-all Upload all locally changed transactions push tracker/project --zip Alternative upload when a WAF blocks the usual request release --version v1.0 --notes \"Tracker baseline\" Create server source snapshot release --list List snapshots rollback --version v1.0 Restore a source snapshot deploy TARGET_FOLDER Copy source into runtime layout under a target directory delete tracker/project Delete remote and local transaction clean Delete the local src tree <p>Quote wildcard arguments such as <code>'tracker/*'</code> so the shell does not expand them. Confirm <code>push-remote</code> arguments with its help before using another server. Source rollback does not restore database rows/schema. <code>deploy</code> requires a target folder and does not create auth SQL, install assets, or roll out Kubernetes.</p>"},{"location":"chapters/12-cli-agents/#use-another-agent-harness-with-the-soad-skill","title":"Use another agent harness with the SOAD skill","text":"<p>The CLI distribution's source bundle includes <code>soad-ai-context/soad-framework/SKILL.md</code> and a <code>reference/</code> directory. Install the whole folder, not only the main Markdown file. The skill is a guide for writing S\u00d8AD code; the <code>soad</code> executable performs synchronization.</p> <p>Different harnesses discover skills differently. Use your harness's documented skill-directory/import mechanism, then ask it to load <code>soad-framework</code> for the workspace. If the harness has no skill loader, explicitly give it the main <code>SKILL.md</code> and the referenced files relevant to the task. This does not require the IDE's built-in AI provider.</p> <p>Before the first write, ask the agent to confirm:</p> <ul> <li>It loaded the skill and recognizes Jython 2.7, generated models, and the CLI layout.</li> <li>It knows the selected development application without printing its credentials.</li> <li>It will create/pull, edit, compare, and push only the transactions in your task.</li> <li>It will keep SQL in migration files for your chosen database execution workflow.</li> <li>It will not treat delete, clean, release, rollback, or deployment as ordinary saves.</li> </ul> <p>Example request:</p> <p>Load the installed soad-framework skill. In this development workspace, pull tracker/task, add a status filter using parameterized queries, preserve ACCESS_POLICY and ownership checks, show the diff, then push the approved change. Do not modify schema or publish a release.</p> <p>Older skill bundles may contain obsolete authentication or rendering examples. Use the current developer guide when an older skill bundle disagrees, and update the installed bundle. After pushing, test the real application session and expected response content; an IDE API token does not authenticate a browser to the runtime application.</p>"},{"location":"chapters/13-testing/","title":"13. Testing and completing the application","text":"<p>Test behavior at the boundary where it matters. A successful compile or HTTP 200 does not prove that a protected application works correctly.</p>"},{"location":"chapters/13-testing/#test-layers","title":"Test layers","text":"Layer What to verify Pure validation Lengths, identifiers, allowed status values Jython and database Generated models, queries, ownership, cascades, rollback HTTP Routing, method guards, authorization, CSRF, redirects, content type Browser Forms, errors, saved values, downloads, narrow-screen usability Deployment Image content, installed schema/manifest, readiness, persistence <p>Run application tests against your container-deployed development environment. Use the S0ADM transaction runner for focused checks, then exercise authenticated HTTP and browser flows against the real application URL. A framework test suite is an operator/product-maintainer concern and does not automatically test the application you create.</p>"},{"location":"chapters/13-testing/#tracker-acceptance-checklist","title":"Tracker acceptance checklist","text":"<ol> <li>Anonymous requests redirect to application login; an IDE session alone is insufficient.</li> <li>Admin and member accounts can create/edit their own projects and tasks.</li> <li>Invalid name/title/status produces feedback and no partial update.</li> <li>IDs for another owner are rejected on list details, edit, save, delete, JSON,    and download paths; a task cannot be moved by submitting another project ID.</li> <li>A member cannot invoke the project-delete route. Administrator access still    obeys ownership.</li> <li>Missing/incorrect CSRF tokens fail on protected mutations. GET requests to    save/delete do not change data.</li> <li>Pagination is stable; filtering stays active when moving between pages.</li> <li>A disabled/nonexistent assignee is rejected. Assignment alone grants no access.</li> <li>Small uploads download as attachments; oversize uploads fail; replacement,    removal, task deletion, and project deletion remove the expected blob rows.</li> <li>HTML-like titles display as text; quotes/newlines in filenames do not create     response headers. JSON contains only selected fields.</li> <li>Failed login, logout, reset, and revoked sessions behave correctly. Test     recovery email and TOTP with disposable accounts.</li> <li>After restart, database records remain; session persistence follows the     deployment's configured storage and expiry.</li> </ol>"},{"location":"chapters/13-testing/#before-release","title":"Before release","text":"<p>Record the framework/CLI version, application source version, business migration version, image digest, and security manifest checksum together. Back up the application database and test restoration. Apply SQL only to the intended environment. Confirm the runtime contains no IDE directory, IDE database, development account credentials, or provider API keys.</p> <p>Use production mode, HTTPS, secure cookies, configured public origin, and bounded upload settings. Exercise <code>/_soad/auth/health</code> and a real protected page after installing auth SQL.</p>"},{"location":"chapters/13-testing/#failure-and-rollback","title":"Failure and rollback","text":"<p>A source rollback is not a database restore. If a deployment changes auth metadata, old and new manifests may not both match the installed checksum. Plan a maintenance window or an explicitly compatible migration; the example's single-replica deployment uses Recreate and permits downtime.</p> <p>For a failed rollout, restore a known image and its compatible metadata/schema. Never rerun an initial business <code>CREATE TABLE</code> script against a populated database as a substitute for a migration.</p>"},{"location":"chapters/14-docker/","title":"14. Deploy with Docker","text":"<p>Build an application-only image, install its database bundles, and create an application administrator. S0ADM belongs on the development instance.</p>"},{"location":"chapters/14-docker/#prepare-an-immutable-application-release","title":"Prepare an immutable application release","text":"<p>Use the release/export workflow supplied by your S0ADM deployment to obtain an application build context containing reviewed transaction source, assets, welcome configuration, security manifest, and SQL bundles. It must not export live users or copy the IDE database.</p> <p>The CLI's <code>deploy TARGET_FOLDER</code> only rearranges transaction source. Use it when your supplied image workflow expects runtime layout, then separately include the exported assets, application libraries, welcome configuration, auth overrides, manifest, and SQL bundles. Do not copy the entire IDE workspace into an application image.</p> <pre><code>flowchart LR\n    Source[Reviewed application source and assets] --&gt; Context[Release context]\n    Policy[Security plan] --&gt; Manifest[Manifest and auth SQL]\n    Manifest --&gt; Context\n    Context --&gt; Image[Application image]\n    Manifest --&gt; Database[(Production database)]\n    Image --&gt; Runtime\n    Runtime --&gt; Database</code></pre> <p>Build from the exported release directory using the runtime base image supplied with your S\u00d8AD installation:</p> <pre><code>cd tracker-release\ndocker build --build-arg BASE_IMAGE=registry.example.com/soad/runtime:VERSION \\\n  -t registry.example.com/apps/tracker:1.0 .\n</code></pre> <p>The supplied application runtime contains the framework and <code>soad-auth-admin</code> but omits S0ADM. Keep the base/application versions coupled; use a private registry and immutable tags or digests for releases.</p>"},{"location":"chapters/14-docker/#local-container-rehearsal","title":"Local container rehearsal","text":"<p>The Compose example is deliberately a localhost HTTP rehearsal, with an isolated MySQL database. Supply <code>TRACKER_DB_PASSWORD</code> and <code>TRACKER_DB_ROOT_PASSWORD</code> through your shell's secret input mechanism or an untracked <code>.env</code> file in the example directory. Do not include that file in source control or release artifacts.</p> <pre><code>cd tracker-example\ndocker compose up -d db\ndocker compose exec db mysql --user=root --password --database=tracker \\\n  -e \"source /install/auth-mysql.sql; source /install/tracker.sql;\"\ndocker compose up -d tracker\ndocker compose exec tracker soad-auth-admin create-admin \\\n  --app default --email admin@example.com --name \"Tracker Administrator\" --password-stdin\n</code></pre> <p>Enter the initial application password on stdin when requested by the command. For automated use, pipe a mounted secret file into <code>docker compose exec -T</code> instead. Do not pass a password argument.</p> <p>Open <code>http://localhost:8081/_soad/auth/login</code>, then <code>/t/tracker/project</code>. Readiness should return 200:</p> <pre><code>curl --fail http://localhost:8081/_soad/auth/health\ndocker compose logs --tail=100 tracker\n</code></pre> <p>The initial SQL commands are for a fresh database only. For subsequent releases apply reviewed migrations and the release's matching auth metadata bundle. <code>docker compose down</code> preserves the named database volume; adding <code>--volumes</code> would delete it. Local container sessions may be lost on container replacement.</p>"},{"location":"chapters/14-docker/#production-configuration","title":"Production configuration","text":"<p>Use an external or properly managed database, database credentials from your secret manager, a TLS reverse proxy, and a public origin:</p> <pre><code>sufia_hostname=https://tracker.example.com\nsufia_app_name=Project Tracker\nsession_cookie_secure=true\nsession_cookie_samesite=LAX\ndb_default_driver_class=com.mysql.cj.jdbc.Driver\ndb_default_url=jdbc:mysql://db.example.internal:3306/tracker?sslMode=VERIFY_IDENTITY\n</code></pre> <p>The database server's certificate chain must be trusted by the JVM for <code>VERIFY_IDENTITY</code>. The local Compose JDBC URL disables TLS solely for its isolated rehearsal network; do not copy it to an untrusted database connection.</p> <p>Set database username/password separately, configure SMTP if using recovery emails, and provide writable directories for generated models, logs, upload temporaries, and session storage. Persist business data in the database. The tracker stores attachments there too.</p>"},{"location":"chapters/14-docker/#upgrade-and-recover","title":"Upgrade and recover","text":"<p>Back up before migrations, retain the prior image digest, and install matching auth SQL before routing traffic to the new runtime. A checksum mismatch causes 503 readiness/access failure. A changed manifest can also make the old image unready, so coordinate metadata updates and image replacement.</p> <p>Use container logs and health checks for diagnostics. The application-only image does not expose S0ADM's log viewer, file editor, or AI service.</p>"},{"location":"chapters/15-kubernetes/","title":"15. Deploy with Kubernetes","text":"<p>The example manifest deploys one tracker replica with a Service, TLS Ingress, configuration, and a session PVC. It assumes an existing MySQL database and an installed Ingress controller.</p>"},{"location":"chapters/15-kubernetes/#prepare-the-cluster-inputs","title":"Prepare the cluster inputs","text":"<ol> <li>Build/push the application image from chapter 14.</li> <li>Create the application database and apply the release's auth SQL and business    migrations using your normal database administration connection.</li> <li>Replace the example image, database hostname, public hostname, Ingress class,    and resource/storage settings. Supply <code>imagePullSecrets</code> if the registry needs it.</li> <li>Create a TLS Secret named <code>tracker-tls</code>, or configure your certificate manager    to create it. Point the public DNS record at the Ingress endpoint.</li> <li>Create the database Secret. The password file below must be a protected local    file containing only the database password (without a trailing newline).</li> </ol> <pre><code>kubectl create namespace tracker\nkubectl -n tracker create secret generic tracker-db \\\n  --from-file=password=/secure/path/tracker-db-password\nkubectl -n tracker apply --dry-run=server -f tracker-kubernetes.yaml\nkubectl -n tracker apply -f tracker-kubernetes.yaml\nkubectl -n tracker rollout status deployment/tracker\n</code></pre> <p>These commands act on the current kubectl context. Check it before applying. The example expects a default dynamic storage class; otherwise specify one on the PVC. Protect/limit Secret access with your cluster's RBAC and secret system.</p>"},{"location":"chapters/15-kubernetes/#traffic-and-readiness","title":"Traffic and readiness","text":"<pre><code>flowchart LR\n    Browser --&gt;|HTTPS| Ingress\n    Ingress --&gt; Service\n    Service --&gt; Pod[Tracker runtime pod]\n    Pod --&gt;|JDBC| DB[(MySQL)]\n    Config[ConfigMap and Secret] --&gt; Pod\n    Pod --&gt; Sessions[(Session PVC)]</code></pre> <p>Startup/liveness use a TCP check for the JVM listener. Readiness uses <code>/_soad/auth/health</code>, which verifies auth schema/manifest compatibility and may fail during a database outage. Keeping database readiness separate from process liveness avoids restarting the JVM repeatedly for a database failure. See the Kubernetes probe guidance.</p> <p>The auth health route is not a complete business-schema diagnostic. Also smoke test the application and inspect logs after rollout.</p>"},{"location":"chapters/15-kubernetes/#initialize-and-verify","title":"Initialize and verify","text":"<p>Create the first administrator on stdin before opening access to the Ingress:</p> <pre><code>kubectl -n tracker exec -i deploy/tracker -- soad-auth-admin create-admin \\\n  --app default --email admin@example.com --name \"Tracker Administrator\" \\\n  --password-stdin &lt; /secure/path/initial-admin-password\nkubectl -n tracker logs deploy/tracker --tail=100\nkubectl -n tracker get pods,pvc,svc,ingress\n</code></pre> <p>The stdin file is a temporary operator secret, not an image input. Check HTTPS sign-in, cookie behavior, project creation, and a private download. Confirm <code>/s0adm/</code> is unavailable.</p>"},{"location":"chapters/15-kubernetes/#storage-and-scaling","title":"Storage and scaling","text":"<p>The runtime generates model classes inside the application's writable filesystem. This example does not set a read-only root filesystem. It runs as UID/GID 1000 and uses <code>fsGroup</code> for the session volume.</p> <p>Jetty's session store is file-based under the JVM temporary directory's <code>s0-session-store</code>. The example persists that path for a single replica. The database also contains revocable application session records; those records alone do not make servlet sessions distributed.</p> <p>Do not raise replicas and assume shared login state. Multi-replica operation requires an explicitly designed servlet-session strategy, storage semantics, and testing; sticky routing alone does not provide failover. The example uses <code>Recreate</code> so two versions do not compete for a ReadWriteOnce volume. This introduces deployment downtime.</p>"},{"location":"chapters/15-kubernetes/#operations","title":"Operations","text":"<p>Set CPU/memory requests and limits from measurements; the sample values are a starting point. Monitor readiness, HTTP errors, latency, JVM memory, pool exhaustion, database capacity, and persistent volume usage.</p> <p>Back up MySQL and verify restore procedures. Retain application image digests, auth SQL, manifests, and migration history. <code>kubectl rollout undo</code> rolls back the Deployment specification, not database state or a changed ConfigMap. Restore compatible metadata/configuration as part of the same recovery plan.</p> <p>The manifest has been supplied as a template; cluster-specific admission, storage, TLS, and database behavior must be verified in your environment.</p>"},{"location":"chapters/16-publishing/","title":"16. Publish application documentation","text":"<p>Application documentation should travel with the application release without requiring access to the S\u00d8AD framework source. Write documentation in your own workspace, publish it through your organization's normal documentation service, and link it from the application where useful.</p>"},{"location":"chapters/16-publishing/#choose-what-to-publish","title":"Choose what to publish","text":"<p>Useful application documentation includes:</p> <ul> <li>user workflows and role-specific capabilities;</li> <li>application URLs and supported browsers;</li> <li>administrator setup and recovery procedures;</li> <li>business-schema migration order;</li> <li>application configuration that operators must supply;</li> <li>health checks, backup expectations, and rollback constraints.</li> </ul> <p>Do not publish IDE credentials, application passwords, API keys, database credentials, invitation tokens, private hostnames, or generated security artifacts containing environment-specific details.</p>"},{"location":"chapters/16-publishing/#keep-release-information-together","title":"Keep release information together","text":"<p>Record the following for each application release:</p> Item Purpose Application version Identifies the transaction and asset release S\u00d8AD runtime version Identifies the compatible runtime container CLI version Helps reproduce source synchronization behavior Image digest Identifies the exact deployed container Business migration version Identifies the expected application schema Security manifest checksum Identifies matching authentication metadata <p>The S0ADM release/export workflow and the <code>soad</code> CLI are the application source boundaries. Documentation may describe their commands, but should not assume the reader has framework internals, build scripts, or the S\u00d8AD product source tree.</p>"},{"location":"chapters/16-publishing/#publish-safely","title":"Publish safely","text":"<p>Build your documentation using the tooling selected by your organization, then upload only its generated public output to the documentation host. Keep source configuration, credentials, local CLI configuration, databases, and temporary release artifacts outside the public document root.</p> <p>For internal applications, apply the same access-control expectations to the documentation site. A public documentation URL can disclose routes, roles, data models, and operational details even when the application itself is protected.</p> <p>After publication, verify links, search, mobile layout, authentication boundaries, and any command examples against the deployed development environment. Update the documentation whenever transaction behavior, roles, permissions, migrations, or runtime requirements change.</p>"},{"location":"examples/tracker/","title":"Complete project/task tracker example","text":"<p>This is a small, complete application accompanying chapter 7. It targets the <code>default</code> application, local authentication, and MySQL. It includes project/task CRUD, owner checks, role-protected deletion, assignment, filtering, pagination, one private attachment per task, and JSON output.</p>"},{"location":"examples/tracker/#source-map","title":"Source map","text":"Input Purpose schema.sql Initial business tables, indexes, foreign keys security-plan.json Roles and permission grants project.py Projects and shared private helpers task.py Tasks, assignment, downloads, JSON layout.py Layout-only transaction layout.html Shared page shell project list, project form Project views task list, task form Task views styles/tracker.css Tailwind input compose.yaml Local container rehearsal kubernetes.yaml Single-replica deployment template"},{"location":"examples/tracker/#browse-the-source","title":"Browse the source","text":"<p>Select a tab to read the example files without leaving this page. The tabs show the files most useful while following the tutorial; the complete source map above links to every supporting asset.</p> SchemaSecurity planProject transactionTask transactionLayoutDocker Compose <pre><code>-- MySQL 8.4; H2 tests use MODE=MySQL. Install framework auth SQL first.\nCREATE TABLE project (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    owner_id VARCHAR(32) NOT NULL,\n    name VARCHAR(120) NOT NULL,\n    FOREIGN KEY (owner_id) REFERENCES soad_auth_user(id)\n);\nCREATE INDEX project_owner ON project(owner_id, id);\nCREATE TABLE task (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    project_id INT NOT NULL,\n    title VARCHAR(200) NOT NULL,\n    status VARCHAR(20) NOT NULL DEFAULT 'todo',\n    assignee_id VARCHAR(32),\n    FOREIGN KEY (project_id) REFERENCES project(id) ON DELETE CASCADE,\n    FOREIGN KEY (assignee_id) REFERENCES soad_auth_user(id),\n    CHECK (status IN ('todo', 'doing', 'done'))\n);\nCREATE INDEX task_project_status ON task(project_id, status, id);\nCREATE TABLE attachment (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    task_id INT NOT NULL UNIQUE,\n    filename VARCHAR(120) NOT NULL,\n    content LONGBLOB NOT NULL,\n    FOREIGN KEY (task_id) REFERENCES task(id) ON DELETE CASCADE\n);\n</code></pre> <pre><code>{\n  \"profile\": \"authenticated\",\n  \"auth_mode\": \"local\",\n  \"registration\": \"invite_only\",\n  \"rationale\": \"Projects and attachments are private to their owners. Route grants do not bypass ownership.\",\n  \"roles\": [\n    {\"code\": \"admin\", \"name\": \"Administrator\"},\n    {\"code\": \"member\", \"name\": \"Member\"}\n  ],\n  \"permissions\": [\n    {\"code\": \"projects.read\", \"roles\": [\"admin\", \"member\"]},\n    {\"code\": \"projects.write\", \"roles\": [\"admin\", \"member\"]},\n    {\"code\": \"projects.delete\", \"roles\": [\"admin\"]},\n    {\"code\": \"tasks.read\", \"roles\": [\"admin\", \"member\"]},\n    {\"code\": \"tasks.write\", \"roles\": [\"admin\", \"member\"]}\n  ]\n}\n</code></pre> <pre><code># -*- coding: utf-8 -*-\nfrom urllib import quote\nfrom models import Project as ProjectModel\nfrom utils import render\nfrom default.tracker.layout import Layout\n\n\ndef _id(value):\n    value = str(value or \"\")\n    if not value.isdigit() or len(value) &gt; 10:\n        return None\n    result = int(value)\n    return result if 0 &lt; result &lt;= 2147483647 else None\n\n\ndef _fail(ctx, status, message):\n    ctx.page_layout = False\n    ctx.go_to = render.as_string(ctx, message)\n\n\ndef _project(ctx, value):\n    identity = _id(value)\n    row = (ProjectModel.first(\"id = ? AND owner_id = ?\", identity,\n                              ctx.output[\"current_user\"][\"id\"])\n           if identity else None)\n    if row is None:\n        _fail(ctx, 404, \"Project not found\")\n    return row\n\n\ndef _page(ctx):\n    return min(_id(ctx.getParameter(\"page\")) or 1, 10000)\n\n\nclass Project(Layout):\n    ACCESS_POLICY = {\n        \"view\": \"permission:projects.read\",\n        \"edit\": \"permission:projects.write\",\n        \"save\": \"permission:projects.write\",\n        \"delete\": \"permission:projects.delete\",\n    }\n\n    def view(self, ctx):\n        query = unicode(ctx.getParameter(\"q\") or \"\").strip()[:120]\n        page = _page(ctx)\n        rows = list(ProjectModel.where(\n            \"owner_id = ? AND name LIKE ?\", ctx.output[\"current_user\"][\"id\"],\n            \"%\" + query + \"%\").orderBy(\"id DESC\").limit(21).offset((page - 1) * 20))\n        base = ctx.ctxPath + \"/t/tracker/project?q=\" + quote(query.encode(\"utf-8\"))\n        ctx.output[\"projects\"] = rows[:20]\n        ctx.output[\"q\"] = query\n        ctx.output[\"page\"] = page\n        ctx.output[\"previous_url\"] = base + \"&amp;page=%d\" % (page - 1) if page &gt; 1 else None\n        ctx.output[\"next_url\"] = base + \"&amp;page=%d\" % (page + 1) if len(rows) &gt; 20 else None\n        ctx.go_to = render.as_view(ctx, \"project\")\n\n    def edit(self, ctx):\n        identity = ctx.getParameter(\"id\")\n        row = _project(ctx, identity) if identity else None\n        if identity and row is None:\n            return\n        ctx.output[\"project\"] = row\n        ctx.go_to = render.as_view(ctx, \"edit\")\n\n    def save(self, ctx):\n        \"\"\"POST\"\"\"\n        identity = ctx.getParameter(\"id\")\n        row = _project(ctx, identity) if identity else None\n        if identity and row is None:\n            return\n        name = unicode(ctx.getParameter(\"name\") or \"\").strip()\n        if not name or len(name) &gt; 120:\n            ctx.output[\"project\"] = {\"id\": identity, \"name\": name}\n            ctx.output[\"error\"] = \"Enter a project name of 1 to 120 characters.\"\n            ctx.go_to = render.as_view(ctx, \"edit\")\n            return\n        if row is None:\n            row = ProjectModel()\n            row.set(\"owner_id\", ctx.output[\"current_user\"][\"id\"])\n        row.set(\"name\", name)\n        row.saveIt()\n        ctx.go_to = ctx.ctxPath + \"/t/tracker/project\"\n\n    def delete(self, ctx):\n        \"\"\"POST\"\"\"\n        row = _project(ctx, ctx.getParameter(\"id\"))\n        if row is None:\n            return\n        row.delete()\n        ctx.go_to = ctx.ctxPath + \"/t/tracker/project\"\n</code></pre> <pre><code># -*- coding: utf-8 -*-\nimport re\nfrom java.sql import Blob\nfrom models import Task as TaskModel, Attachment, Soad_auth_user\nfrom default.tracker.project import _id, _fail, _project, _page\nfrom default.tracker.layout import Layout\nfrom utils import render\n\n\ndef _task(ctx, project, identity):\n    row = TaskModel.first(\"id = ? AND project_id = ?\", _id(identity) or -1,\n                          project.getId())\n    if row is None:\n        _fail(ctx, 404, \"Task not found\")\n    return row\n\n\ndef _form(ctx, project, task, error=None):\n    ctx.output[\"project\"] = project\n    ctx.output[\"task\"] = task\n    # Never pass entire authentication models (including password fields) to a view.\n    ctx.output[\"people\"] = [\n        {\"id\": user.getId(), \"full_name\": user.get(\"full_name\")}\n        for user in Soad_auth_user.where(\"status = ?\", \"active\").orderBy(\"full_name ASC\")]\n    ctx.output[\"error\"] = error\n    ctx.go_to = render.as_view(ctx, \"edit\")\n\n\nclass Task(Layout):\n    ACCESS_POLICY = {\n        \"view\": \"permission:tasks.read\",\n        \"data\": \"permission:tasks.read\",\n        \"download\": \"permission:tasks.read\",\n        \"edit\": \"permission:tasks.write\",\n        \"save\": \"permission:tasks.write\",\n        \"delete\": \"permission:tasks.write\",\n    }\n\n    def _rows(self, ctx, project):\n        status = str(ctx.getParameter(\"status\") or \"\")\n        if status and status not in (\"todo\", \"doing\", \"done\"):\n            _fail(ctx, 400, \"Invalid task status\")\n            return None\n        page = _page(ctx)\n        query = TaskModel.where(\"project_id = ?\", project.getId())\n        if status:\n            query = TaskModel.where(\"project_id = ? AND status = ?\", project.getId(), status)\n        rows = list(query.orderBy(\"id DESC\").limit(21).offset((page - 1) * 20))\n        base = \"%s/t/tracker/task?project_id=%s&amp;status=%s\" % (\n            ctx.ctxPath, project.getId(), status)\n        ctx.output[\"status\"] = status\n        ctx.output[\"page\"] = page\n        ctx.output[\"previous_url\"] = base + \"&amp;page=%d\" % (page - 1) if page &gt; 1 else None\n        ctx.output[\"next_url\"] = base + \"&amp;page=%d\" % (page + 1) if len(rows) &gt; 20 else None\n        return rows[:20]\n\n    def view(self, ctx):\n        project = _project(ctx, ctx.getParameter(\"project_id\"))\n        if project is None:\n            return\n        rows = self._rows(ctx, project)\n        if rows is None:\n            return\n        ctx.output[\"project\"] = project\n        ctx.output[\"tasks\"] = rows\n        ctx.go_to = render.as_view(ctx, \"task\")\n\n    def data(self, ctx):\n        project = _project(ctx, ctx.getParameter(\"project_id\"))\n        if project is None:\n            return\n        rows = self._rows(ctx, project)\n        if rows is None:\n            return\n        ctx.page_layout = False\n        ctx.go_to = render.as_json(ctx, {\"tasks\": [\n            {\"id\": row.getId(), \"title\": row.get(\"title\"), \"status\": row.get(\"status\")}\n            for row in rows], \"page\": ctx.output[\"page\"],\n            \"has_next\": bool(ctx.output.get(\"next_url\"))})\n\n    def edit(self, ctx):\n        project = _project(ctx, ctx.getParameter(\"project_id\"))\n        if project is None:\n            return\n        identity = ctx.getParameter(\"id\")\n        row = _task(ctx, project, identity) if identity else None\n        if identity and row is None:\n            return\n        _form(ctx, project, row)\n\n    def save(self, ctx):\n        \"\"\"POST\"\"\"\n        project = _project(ctx, ctx.getParameter(\"project_id\"))\n        if project is None:\n            return\n        identity = ctx.getParameter(\"id\")\n        row = _task(ctx, project, identity) if identity else None\n        if identity and row is None:\n            return\n        title = unicode(ctx.getParameter(\"title\") or \"\").strip()\n        status = str(ctx.getParameter(\"status\") or \"\")\n        assignee = str(ctx.getParameter(\"assignee_id\") or \"\").strip()\n        draft = {\"id\": identity, \"title\": title, \"status\": status, \"assignee_id\": assignee}\n        error = None\n        if not title or len(title) &gt; 200:\n            error = \"Enter a task title of 1 to 200 characters.\"\n        elif status not in (\"todo\", \"doing\", \"done\"):\n            error = \"Choose todo, doing, or done.\"\n        elif assignee and Soad_auth_user.first(\"id = ? AND status = ?\", assignee, \"active\") is None:\n            error = \"Choose an active application user.\"\n        # WebContext.getParameter returns Java String; files require the wrapper.\n        upload = ctx.getRequest().getParameter(\"attachment\")\n        if upload and (not hasattr(upload, \"getBytes\") or upload.getSize() &gt; 1048576):\n            error = \"Choose a file no larger than 1 MiB.\"\n        if error:\n            _form(ctx, project, draft, error)\n            return\n        if row is None:\n            row = TaskModel()\n            row.set(\"project_id\", project.getId())\n        row.set(\"title\", title)\n        row.set(\"status\", status)\n        row.set(\"assignee_id\", assignee or None)\n        row.saveIt()\n        existing = Attachment.first(\"task_id = ?\", row.getId())\n        if ctx.getParameter(\"remove_attachment\") == \"yes\" and existing is not None:\n            existing.delete()\n            existing = None\n        if upload:\n            attached = existing if existing is not None else Attachment()\n            attached.set(\"task_id\", row.getId())\n            # ASCII, bounded, and safe for a quoted Content-Disposition filename.\n            filename = re.sub(r\"[^A-Za-z0-9._-]\", \"_\", unicode(upload.getFileName()))[:120]\n            attached.set(\"filename\", filename if filename.strip(\".\") else \"download.bin\")\n            attached.set(\"content\", upload.getBytes())\n            attached.saveIt()\n        ctx.go_to = \"%s/t/tracker/task?project_id=%s\" % (ctx.ctxPath, project.getId())\n\n    def download(self, ctx):\n        project = _project(ctx, ctx.getParameter(\"project_id\"))\n        if project is None:\n            return\n        row = _task(ctx, project, ctx.getParameter(\"id\"))\n        if row is None:\n            return\n        attached = Attachment.first(\"task_id = ?\", row.getId())\n        if attached is None:\n            _fail(ctx, 404, \"No attachment\")\n            return\n        ctx.page_layout = False\n        ctx.getResponse().setHeader(\"X-Content-Type-Options\", \"nosniff\")\n        ctx.getResponse().setHeader(\"Cache-Control\", \"no-store\")\n        content = attached.get(\"content\")\n        if isinstance(content, Blob):\n            content = content.getBytes(1, int(content.length()))\n        ctx.go_to = render.as_blob(ctx, content,\n                                  \"application/octet-stream\", attached.get(\"filename\"),\n                                  attachment=True)\n\n    def delete(self, ctx):\n        \"\"\"POST\"\"\"\n        project = _project(ctx, ctx.getParameter(\"project_id\"))\n        if project is None:\n            return\n        row = _task(ctx, project, ctx.getParameter(\"id\"))\n        if row is None:\n            return\n        row.delete()\n        ctx.go_to = \"%s/t/tracker/task?project_id=%s\" % (ctx.ctxPath, project.getId())\n</code></pre> <pre><code>class Layout(object):\n    ACCESS_POLICY = {\"view\": \"authenticated\", \"page_layout\": \"authenticated\"}\n\n    def page_layout(self, ctx):\n        return (\"tracker\", \"layout\", \"layout\")\n\n    def view(self, ctx):\n        # Shared layout data can be prepared here.\n        pass\n</code></pre> <pre><code># Run from this directory after preparing ./release and building tracker:1.0.\nservices:\n  db:\n    image: mysql:8.4\n    environment:\n      MYSQL_DATABASE: tracker\n      MYSQL_USER: tracker\n      MYSQL_PASSWORD: ${TRACKER_DB_PASSWORD:?Set TRACKER_DB_PASSWORD}\n      MYSQL_ROOT_PASSWORD: ${TRACKER_DB_ROOT_PASSWORD:?Set TRACKER_DB_ROOT_PASSWORD}\n    volumes:\n      - tracker-db:/var/lib/mysql\n      - ./release/webapp/WEB-INF/deploy:/install:ro\n    healthcheck:\n      test: [\"CMD\", \"mysqladmin\", \"ping\", \"-h\", \"127.0.0.1\", \"--silent\"]\n      interval: 5s\n      timeout: 3s\n      retries: 30\n  tracker:\n    image: tracker:1.0\n    depends_on:\n      db:\n        condition: service_healthy\n    ports:\n      - \"127.0.0.1:8081:8080\"\n    environment:\n      db_default_url: jdbc:mysql://db:3306/tracker?useSSL=false&amp;allowPublicKeyRetrieval=true\n      db_default_driver_class: com.mysql.cj.jdbc.Driver\n      db_default_username: tracker\n      db_default_password: ${TRACKER_DB_PASSWORD:?Set TRACKER_DB_PASSWORD}\n      sufia_app_name: Project Tracker\n      sufia_hostname: http://localhost:8081\n      session_cookie_secure: \"false\"\n      session_cookie_samesite: LAX\n    restart: unless-stopped\nvolumes:\n  tracker-db:\n</code></pre>"},{"location":"examples/tracker/#install-in-the-ide","title":"Install in the IDE","text":"<ol> <li>Use a fresh default application database. Initialize local authenticated    security and the admin/member roles, then configure permissions exactly as    shown in the JSON plan using the application Roles and permissions editor.</li> <li>Apply schema.sql after the auth schema exists, then Introspect.</li> <li>Create tracker/layout, tracker/project, and tracker/task in that order.    Paste their source and views into the matching transaction editor files.</li> <li>Upload the compiled stylesheet to assets/tracker/tracker.css with File Manager.</li> <li>Set the Welcome transaction to tracker/project. Sign in using an application    account, then open /t/tracker/project.</li> </ol> <p>For CLI editing, create the transactions remotely first, then copy each controller/view into its nested src/group/code location and push it. Global assets and database SQL use their own upload/migration workflow.</p>"},{"location":"examples/tracker/#build-the-css","title":"Build the CSS","text":"<p>In a local copy of the example assets:</p> <pre><code>cd tracker-example\nnpm ci\nnpm run build:css\n</code></pre> <p>The compiled stylesheet is included for direct use. It has no browser CDN dependency. Node/Tailwind are build tools; they are not needed by the deployed JVM.</p>"},{"location":"examples/tracker/#prepare-a-container-release","title":"Prepare a container release","text":"<p>Create the tracker in S0ADM or synchronize it with the <code>soad</code> CLI, then use the release/export workflow supplied by your IDE deployment. The exported release must include the transaction sources, compiled stylesheet, welcome selection, signed security manifest, and matching authentication SQL. Use the Docker chapter for the build and database installation sequence.</p>"},{"location":"examples/tracker/#verification","title":"Verification","text":"<p>Test the deployed development application with disposable application accounts. Exercise CSRF, ownership, role denial, form validation, escaping, CRUD, assignment, pagination, JSON, uploads, downloads, and deletion. This does not replace testing production-compatible MySQL, real SMTP/TOTP delivery, browser interactions, or a Docker/Kubernetes deployment in your target environment.</p>"},{"location":"examples/tracker/#deliberate-limits","title":"Deliberate limits","text":"<p>Assignment does not share access; only the project owner sees its records. The administrator deletion permission does not bypass ownership. Accounts are managed through built-in auth, and business rows do not hold credentials.</p> <p>Each task has at most one nonempty attachment of at most 1 MiB. Uploading replaces the existing attachment; selecting remove and uploading together replaces it. Empty file selection preserves the old attachment. After validation errors, the browser requires you to choose the file again.</p> <p>Writes use normal request transactions. The tutorial uses last-write-wins; add optimistic concurrency if your workflow needs conflict detection. It has no background notification worker or malware-scanning integration.</p>"},{"location":"reference/ai-assistant/","title":"AI assistant workflows","text":"<p>The S\u00d8AD IDE includes an AI assistant that builds and edits transactions for you from a plain-language description. You tell it what you want \u2014 \"create a contact CRUD\", \"add an email field to the signup form\", \"why is the invoice page throwing an error?\" \u2014 and it uses the same building blocks you do (transactions, views, the database schema) to do the work.</p> <p>Ordinary transaction-editor source changes are staged for review. The IDE also supports optional auto-accept and a separate whole-app generation workflow that can execute SQL and install provisional source. Read S0ADM and AI before using these modes against valuable data.</p>"},{"location":"reference/ai-assistant/#enabling-the-assistant","title":"Enabling the assistant","text":"<p>The assistant talks to a model that you provide (bring-your-own-key), through one of three provider families:</p> Provider Wire protocol Use it for OpenAI compatible <code>/chat/completions</code> OpenAI, Azure OpenAI, OpenRouter, DeepSeek, and local servers such as Ollama, vLLM and LM Studio OpenAI Responses <code>/responses</code> OpenAI's Responses API Anthropic <code>/messages</code> Claude models <p>Whichever you pick, you configure the same four things:</p> Setting Description Example AI features Master switch. When disabled, the AI button, the Edit with AI link and the dashboard prompt are all hidden <code>Enabled</code> Endpoint The provider's base URL (the request path is appended automatically) <code>https://api.anthropic.com/v1</code> API key Your provider key <code>sk-ant-\u2026</code> Model The model name to use <code>claude-sonnet-4-5</code> <p>Leaving the endpoint blank is fine for the OpenAI Responses and Anthropic families \u2014 they default to <code>https://api.openai.com/v1</code> and <code>https://api.anthropic.com/v1</code>. An OpenAI-compatible provider always needs an explicit endpoint, since the endpoint is the choice of gateway.</p> <p>There are two ways to set them.</p>"},{"location":"reference/ai-assistant/#from-the-ide-self-hosted","title":"From the IDE (self-hosted)","text":"<p>Go to System Settings \u2192 Applications \u2192 &lt;your application&gt; \u2192 AI assistant (superadmin only), pick the provider, fill in the endpoint, API key and model, and save. This is the quickest way on a self-hosted instance. The settings are platform-wide: one provider serves every application, whichever application's page you open them from.</p>"},{"location":"reference/ai-assistant/#from-configuration-environment-managed","title":"From configuration / environment (managed)","text":"<p>Set them in <code>sufia.properties</code> or as environment variables \u2014 the usual way to inject config in a container or Kubernetes deployment.</p> Property Environment variable <code>ai.enabled</code> <code>ai_enabled</code> <code>ai.provider</code> <code>ai_provider</code> <code>ai.endpoint</code> <code>ai_endpoint</code> <code>ai.api.key</code> <code>ai_api_key</code> <code>ai.model</code> <code>ai_model</code> <code>ai.auto.accept</code> <code>ai_auto_accept</code> <p><code>ai.provider</code> accepts <code>openai</code>, <code>openai-responses</code> or <code>anthropic</code>; any other value (a gateway brand name such as <code>deepseek</code>) is treated as OpenAI-compatible. <code>ai.enabled</code> defaults to <code>true</code> \u2014 set it to <code>false</code> to ship an instance with no AI entry points at all.</p> <p>When the same value is set in more than one place, the order of precedence is, per field:</p> <p>in-IDE AI assistant settings \u2192 environment variable \u2192 <code>sufia.properties</code> \u2192 built-in default.</p> <p>Note</p> <p>If the assistant reports that it is not configured, no endpoint / key / model has been resolved from any of the sources above.</p>"},{"location":"reference/ai-assistant/#choosing-ai-run-limits","title":"Choosing AI run limits","text":"<p>Three settings have an outsized effect on whether the assistant can finish a large task reliably:</p> Setting What it limits When to tune it <code>ai.turn.cap</code> (<code>ai_turn_cap</code>) Model and tool-use round trips for ordinary editor chat Raise for a genuinely larger edit; lower it for a less capable model that starts repeating itself. <code>ai.one.shot.turn.cap</code> (<code>ai_one_shot_turn_cap</code>) Round trips for whole-app generation from an empty workspace This is intentionally separate and normally higher because a complete app needs planning, several writes, and validation. <code>ai.context.max.tokens</code> (<code>ai_context_max_tokens</code>) Prior chat and tool history included in each model request Raise for a large-context model so it retains more of a long build; keep enough room for instructions, tool results, and the next response. <p>A turn is one model response and its resulting tool work, not one visible chat message. Creating a transaction often takes roughly three turns. Both turn caps are safety limits: they stop an unattended run from looping forever, especially with a smaller or less reliable model. Increase them only for work that needs additional steps.</p> <p>When history exceeds <code>ai.context.max.tokens</code>, the oldest complete turns are trimmed from the request, though the full transcript remains saved. Do not set the budget to the model's advertised context window: the system prompt, tool calls/results, and output all consume that window too. For example, <code>200000</code> tokens is a useful starting point for a 1M-context model; models with a smaller window need a lower, conservative budget. See the configuration reference for the property defaults and all AI settings.</p>"},{"location":"reference/ai-assistant/#opening-the-assistant","title":"Opening the assistant","text":"<p>Open a transaction in the editor and click the AI button in the toolbar to slide out the chat panel. You can also click Edit with AI in the editor footer to open the panel pre-focused on the transaction you're viewing.</p> <p>Type what you want and send (\u2318/Ctrl + Enter). The assistant streams its thinking and shows each step it takes (\"Reading schema for table <code>contact</code>\", \"Writing <code>contact.py</code>\", and so on).</p>"},{"location":"reference/ai-assistant/#the-approval-gate","title":"The approval gate","text":"<p>In ordinary editor chat with auto-accept disabled, prepared source changes appear in a review with a before/after diff for every file:</p> <ul> <li>Approve &amp; Execute \u2014 write the changes to the server (and, for a new   transaction, open it in the editor).</li> <li>Discard \u2014 throw the staged work away.</li> </ul> <p>Workflow differences</p> <p>Auto-accept defaults off; enabling it can automate eligible approval actions. Whole-app generation can mutate the schema and provisional source before final publication. Rename/move and project-memory operations have their own behavior and are not ordinary staged source edits.</p>"},{"location":"reference/ai-assistant/#what-you-can-ask-it-to-do","title":"What you can ask it to do","text":""},{"location":"reference/ai-assistant/#create-a-transaction","title":"Create a transaction","text":"<p>Describe the transaction you want. If you don't say which group and code to use, the assistant asks first rather than guessing \u2014 so the URL and file layout are what you expect.</p> <p>\"Create a CRUD for the <code>product</code> table in group <code>catalog</code>.\"</p>"},{"location":"reference/ai-assistant/#build-a-whole-app","title":"Build a whole app","text":"<p>Ask for several transactions at once and the assistant builds and wires them together in one go. For a multi-part request it chooses sensible group/code names itself.</p> <p>\"Build a booking app: a room list, a booking form, and a room CRUD.\"</p> <p>The review shows each transaction as its own card; Approve &amp; Execute commits them together.</p>"},{"location":"reference/ai-assistant/#security-for-generated-apps","title":"Security for generated apps","text":"<p>For a whole-app request, the assistant classifies the app as <code>public</code>, <code>authenticated</code>, or <code>mixed</code>, then includes explicit action policies, roles, permissions, login/logout behavior, and CSRF protection in the plan. Internal business apps default to application-local identity; landing pages and other intentionally public apps do not receive unnecessary user management.</p> <p>Administrators can later keep the action policies and replace local identity with a custom application provider. See Application Security and Authentication Providers for the inference rules, provider contract, switching procedure, rollback, and troubleshooting.</p>"},{"location":"reference/ai-assistant/#edit-an-existing-transaction","title":"Edit an existing transaction","text":"<p>Open a transaction (or use Edit with AI) and describe the change. The assistant reads the current files first and stages a focused edit.</p> <p>\"Add an <code>email</code> column to the form and validate it.\"</p>"},{"location":"reference/ai-assistant/#rename-or-move-a-transaction","title":"Rename or move a transaction","text":"<p>Ask it to change a transaction's group and/or code. Because a rename also updates the controller class name and the default view, the assistant states the exact rename and asks you to confirm in chat first \u2014 and then applies it immediately.</p> <p>\"Rename <code>orders/cart</code> to <code>shop/basket</code>.\"</p> <p>Note</p> <p>A rename doesn't rewrite your own internal references (links, explicit view names). The assistant will remind you to check those.</p>"},{"location":"reference/ai-assistant/#roll-back-to-a-previous-version","title":"Roll back to a previous version","text":"<p>Every save is versioned. If a change didn't work out, ask the assistant to restore an earlier version. It lists the saved versions, you pick one, and it stages that version as a change so you review the diff before approving. Approving snapshots the current state first, so a restore is itself reversible.</p> <p>\"Show me the versions of <code>catalog/product</code> and restore yesterday's.\"</p>"},{"location":"reference/ai-assistant/#diagnose-data-problems","title":"Diagnose data problems","text":"<p>When something looks wrong (a missing record, an unexpected value), the assistant can run read-only queries to inspect the actual rows before suggesting a fix.</p> <p>\"The dashboard total is wrong \u2014 can you check the <code>orders</code> data?\"</p>"},{"location":"reference/ai-assistant/#debug-runtime-errors","title":"Debug runtime errors","text":"<p>If a page is throwing or misbehaving, reproduce it, then ask the assistant. It reads the server log to find the real error and stack trace, then proposes a fix based on what actually happened instead of guessing.</p> <p>\"The invoice page returns a 500 \u2014 what's wrong?\"</p>"},{"location":"reference/ai-assistant/#schema-changes-and-sql","title":"Schema changes and SQL","text":"<p>In ordinary editor chat the assistant can read schema and suggest SQL for the developer's Execute button. Authorized whole-app generation additionally has a direct SQL execution path. For a normal editor SQL block:</p> <ul> <li>Review the SQL.</li> <li>Click Execute to run it against your application database.</li> <li>After a structural change the models are refreshed automatically, so new   columns and tables are immediately available.</li> </ul> <p>Warning</p> <p>For this editor workflow, read SQL before clicking Execute \u2014 destructive statements (for example <code>DROP</code>) are blocked, but you are still the one applying the change.</p>"},{"location":"reference/ai-assistant/#project-memory-agentsmd","title":"Project memory (<code>AGENTS.md</code>)","text":"<p>The assistant already knows the S\u00d8AD framework, but it doesn't know your project's conventions. You can teach it by keeping notes in a file at <code>WEB-INF/AGENTS.md</code>. Anything in that file \u2014 design decisions, naming conventions, UI guidelines, domain rules \u2014 is treated as authoritative guidance on every request.</p> <p>Two ways to maintain it:</p> <ul> <li>Edit it yourself in the File Manager (it's a normal Markdown file).</li> <li>Ask the assistant to remember something \u2014 say \"remember this\" or \"save to   memory\" and it appends the note for you.</li> </ul> <p>\"Remember: all money amounts are stored in cents, and we always soft-delete.\"</p> <p><code>AGENTS.md</code> is optional. When it's empty, the assistant just uses its built-in framework knowledge.</p>"},{"location":"reference/ai-assistant/#designing-larger-apps-with-a-planner","title":"Designing larger apps with a planner","text":"<p>The assistant is a builder \u2014 it's at its best turning a clear, focused request into transactions you approve. For a larger app \u2014 many screens, a real data model, cross-cutting conventions \u2014 it pays to design first, then build.</p> <p>A workflow that works well:</p> <ol> <li>Plan with a capable \"frontier\" AI. Use a strong general-purpose model (the    kind you'd reach for to architect and design) to turn your idea into a concrete    plan: the data model, the list of transactions, how they link, and your    conventions.</li> <li>Give the planner S\u00d8AD's context. So its plan uses S\u00d8AD conventions rather than    generic ones, provide it with S\u00d8AD's coding context (the framework documentation)    and the planning guide below \u2014 the guide tells it how to shape a plan the    assistant can build cleanly.</li> <li>Split the result into two streams:<ul> <li>Durable conventions (naming, money handling, soft-delete, UI standards) go   into your project's <code>AGENTS.md</code> once \u2014 they then apply to every request.</li> <li>The build plan (data model + transactions) goes into the chat, fed   phase by phase.</li> </ul> </li> <li>Build in order: run the schema SQL first (Execute), then paste the    transactions a batch at a time, approving each before moving on.</li> </ol> <p>This split isn't arbitrary: <code>AGENTS.md</code> is always in the assistant's context, while chat history is finite and trims older turns. Put \"always true\" in <code>AGENTS.md</code> and \"what to build now\" in chat.</p> <p>Planning guide</p> <p>Give this to your planning AI, alongside S\u00d8AD's coding context, so it produces plans shaped for the assistant to build:</p> <p>Read the S\u00d8AD Planning Guide</p>"},{"location":"reference/ai-assistant/#conversations","title":"Conversations","text":"<p>The panel keeps your chat history per transaction, so you can close it and come back later. Use New chat to start a fresh conversation (clearing the context), and All conversations to browse past chats.</p>"},{"location":"reference/ai-assistant/#safety-and-scope","title":"Safety and scope","text":"<p>The assistant is deliberately constrained:</p> <ul> <li>Ordinary editor file changes use staged review; optional auto-accept changes   the interaction. Rename/move and project-memory updates are separate operations.</li> <li>Ordinary editor SQL is developer-executed. Server-authorized empty-app   generation can execute SQL and install provisional transactions.</li> <li>Transaction version restoration stages source changes. It is not a database   restore or production release rollback. Use the release/deployment tools for   those separate operational workflows.</li> </ul> <p>What is sent to the model</p> <p>To do its work, the assistant sends relevant context to your configured model provider \u2014 your request, the transaction files it's working on, schema details, and, when debugging, log excerpts. Choose a provider you trust with your project data, and be aware that anything present in the server log (which the log tool may read) can be included when you ask it to debug.</p>"},{"location":"reference/ai-assistant/#tips-for-good-results","title":"Tips for good results","text":"<ul> <li>Be specific about names. Give the group and code when you have a preference;   otherwise the assistant will ask.</li> <li>Mention the tables and columns involved \u2014 the assistant reads the schema, but   naming them removes ambiguity.</li> <li>Work in small steps and approve as you go; it's easier to review a focused   diff than a sweeping one.</li> <li>Reproduce an error first, then ask \u2014 the assistant reads the log, so a fresh   error trail helps it find the cause.</li> <li>Capture conventions in <code>AGENTS.md</code> once, instead of repeating them in every   chat.</li> <li>Plan big apps first. For anything large, design it with a planner and feed it   in phases \u2014 see Designing larger apps with a planner.</li> </ul>"},{"location":"reference/ai-assistant/#troubleshooting","title":"Troubleshooting","text":"Symptom Likely cause / fix \"AI assistant is not configured\" No endpoint / key / model resolved \u2014 set them in System Settings \u2192 Applications \u2192 AI assistant or via the <code>ai.*</code> properties/environment variables. No AI button, no dashboard prompt The feature is switched off \u2014 set AI features to Enabled, or <code>ai.enabled = true</code> / <code>ai_enabled=true</code>. Requests fail with an auth error Wrong API key, or the model name isn't valid for your provider. Nothing happens / provider error Check the endpoint base URL, and that the selected provider matches the API the endpoint actually speaks (<code>/chat/completions</code>, <code>/responses</code> or <code>/messages</code>). Claude replies are cut off mid-file Raise <code>ai.max.output.tokens</code> (Anthropic defaults to 8192 output tokens). A change didn't take effect Confirm you clicked Approve &amp; Execute \u2014 staged changes aren't written until you do. The model doesn't see a new column/table Run the suggested SQL with Execute (which refreshes the models), or Introspect the datasource."},{"location":"reference/ai-planning/","title":"Planning applications with AI","text":"<p>Read this together with the developer chapters. They teach the S\u00d8AD framework (Jython transactions, Handlebars views, ActiveJDBC models, routing, hard rules). This file tells you how to turn a request into a build plan that a second AI \u2014 the S\u00d8AD IDE agent \u2014 will implement.</p> <p>You are the architect. You do not write the final code here; you produce a clear, staged plan. The agent is the builder: it reads your plan, generates the transactions. Specify the intended review points and generation mode in the plan.</p>"},{"location":"reference/ai-planning/#how-the-builder-works-plan-within-these-constraints","title":"How the builder works (plan within these constraints)","text":"<ul> <li>Decompose the app into transaction-sized units, each doing one job.   Ordinary editor source changes are staged; whole-app generation can coordinate   multiple transactions and install provisional source during validation.</li> <li>Put schema SQL before dependent controllers. Editor-chat SQL uses the   developer Execute action; server-authorized whole-app generation can execute   DDL/DML. State which workflow and database the plan targets.</li> <li>It asks for <code>group</code>/<code>code</code> if you don't specify them (for a single   transaction). So always name the group and code for every transaction.</li> <li>It already knows S\u00d8AD idioms \u2014 routing, the POST/PRG pattern, model naming,   default-view resolution, the built-in helpers. Specify intent, not mechanics.   Don't re-teach the framework or hand-write boilerplate the agent will produce   correctly on its own.</li> <li>Phase the plan into reviewable chunks. Account for the selected auto-accept   setting and whole-app workflow rather than promising a click before every write.</li> <li>Its context is bounded. Keep the plan tight and concrete; for a big app, split   it so it can be fed and approved in stages rather than one huge block.</li> <li>Separate development from release operations. A source restoration is not   a database rollback; deployment needs its own migration and verification plan.</li> </ul>"},{"location":"reference/ai-planning/#what-to-produce","title":"What to produce","text":"<p>Output a plan with these parts, in this order:</p> <ol> <li>Data model \u2014 the tables, columns and relationships, with the <code>CREATE TABLE</code>    SQL when the tables don't already exist. This runs first (human clicks Execute).</li> <li>Transactions \u2014 one block per transaction (see the template). This is the bulk    of the plan and maps 1:1 onto what the builder creates.</li> <li>Phases \u2014 group the transactions into approval-sized batches, in build order    (schema \u2192 core entities \u2192 screens that depend on them \u2192 wiring).</li> <li>Project conventions (optional) \u2014 durable rules the builder should always    follow (naming, money handling, soft-delete, UI conventions). These belong in the    project's <code>AGENTS.md</code>, not repeated in every request \u2014 call them out separately.</li> </ol>"},{"location":"reference/ai-planning/#style-rules-what-makes-a-plan-land-well","title":"Style rules (what makes a plan land well)","text":"<ul> <li>Name <code>group/code</code> explicitly for every transaction \u2014 it becomes the URL   <code>/t/&lt;group&gt;/&lt;code&gt;</code> and the file layout. Never leave it to the builder to guess.</li> <li>Name tables and columns. The builder reads the schema, but naming them removes   ambiguity and prevents wrong assumptions.</li> <li>Schema first. If tables don't exist, give the DDL up front so the build targets   real columns.</li> <li>One responsibility per transaction. Prefer <code>catalog/room</code> (CRUD) + <code>booking/new</code>   (a form) over one mega-transaction.</li> <li>Specify behaviour, not framework code. Say \"list with a link to the form,   soft-delete on the row\" \u2014 not how to wire <code>render.as_view</code> or the PRG redirect.</li> <li>Describe the actions each transaction needs (e.g. <code>view</code>, <code>list</code>, <code>save</code>,   <code>delete</code>) and any per-action method (a save is a POST).</li> <li>State the fields and validation (required, type, unique) \u2014 these drive both the   form and the controller.</li> <li>Wire the screens together. Say which list links to which form/detail, so the   built transactions form one app rather than isolated pages.</li> <li>Phase to approval batches. \"These 3 transactions\" is one reviewable diff; a   20-transaction dump is not.</li> <li>Flag anything that needs SQL (schema change, seed data) as a fenced <code>sql</code>   block for a human to Execute \u2014 never assume the builder ran it.</li> </ul>"},{"location":"reference/ai-planning/#plan-template","title":"Plan template","text":"<p>Produce your plan in this shape (fill in / repeat the transaction block per screen):</p> <pre><code># &lt;App name&gt; \u2014 build plan\n\n## Data model\n- table `&lt;name&gt;` (&lt;col&gt; &lt;type&gt; [pk|required|unique|-&gt; other_table], ...)\n  ...\n### Schema SQL (run first, via Execute)\n```sql\nCREATE TABLE ...;\n</code></pre>"},{"location":"reference/ai-planning/#transactions","title":"Transactions","text":""},{"location":"reference/ai-planning/#_1","title":"/<code> <p>Purpose: . Actions: . Fields: . Views: . Links: . Notes: .","text":""},{"location":"reference/ai-planning/#_2","title":"/<code> <p>...</p>","text":""},{"location":"reference/ai-planning/#phases-build-approval-order","title":"Phases (build &amp; approval order)","text":"<ol> <li>Schema SQL (Execute).</li> <li> \u2014 approve. <li> \u2014 approve. ..."},{"location":"reference/ai-planning/#project-conventions-put-these-in-agentsmd","title":"Project conventions (put these in AGENTS.md)","text":"<ul> <li> <li>... <pre><code>---\n\n## Worked example (short)\n</code></pre></li>"},{"location":"reference/ai-planning/#room-booking-build-plan","title":"Room booking \u2014 build plan","text":""},{"location":"reference/ai-planning/#data-model","title":"Data model","text":"<ul> <li>table <code>room</code> (id pk, name required, capacity int, active bool)</li> <li>table <code>booking</code> (id pk, room_id -&gt; room, guest_name required, start_date, end_date)</li> </ul>"},{"location":"reference/ai-planning/#schema-sql-run-first-via-execute","title":"Schema SQL (run first, via Execute) <pre><code>CREATE TABLE room (\n  id INT AUTO_INCREMENT PRIMARY KEY,\n  name VARCHAR(100) NOT NULL,\n  capacity INT,\n  active BOOLEAN DEFAULT TRUE\n);\nCREATE TABLE booking (\n  id INT AUTO_INCREMENT PRIMARY KEY,\n  room_id INT,\n  guest_name VARCHAR(120) NOT NULL,\n  start_date DATE,\n  end_date DATE,\n  FOREIGN KEY (room_id) REFERENCES room(id)\n);\n</code></pre>","text":""},{"location":"reference/ai-planning/#transactions_1","title":"Transactions","text":""},{"location":"reference/ai-planning/#catalogroom","title":"catalog/room <p>Purpose: manage rooms (CRUD). Actions: list, view, save (POST), delete (DELETE). Fields: name (required), capacity (int), active (bool). Views: a table of rooms, each row linking to the edit form; a simple add/edit form. Links: list is the entry point; form returns to the list after save.</p>","text":""},{"location":"reference/ai-planning/#bookingnew","title":"booking/new <p>Purpose: create a booking for a room. Actions: view, save (POST). Fields: room (select from <code>room</code> where active), guest_name (required),         start_date, end_date (end after start). Views: a form; on save, redirect back to the room list with a success message. Links: reached from the catalog/room list (\"Book\" action).</p>","text":""},{"location":"reference/ai-planning/#phases","title":"Phases","text":"<ol> <li>Schema SQL (Execute).</li> <li>catalog/room \u2014 approve.</li> <li>booking/new \u2014 approve.</li> </ol>"},{"location":"reference/ai-planning/#project-conventions-put-these-in-agentsmd_1","title":"Project conventions (put these in AGENTS.md)","text":"<ul> <li>Dates are stored as DATE; display as YYYY-MM-DD.</li> <li>Lists use the standard table layout; forms use a single-column card. ```</li> </ul>"},{"location":"reference/ai-planning/#hand-off-checklist","title":"Hand-off checklist","text":"<ul> <li>[ ] Every transaction has an explicit <code>group/code</code>.</li> <li>[ ] Tables/columns are named; DDL is included if they don't exist yet.</li> <li>[ ] Schema SQL comes first and is marked \"Execute\".</li> <li>[ ] Transactions are single-purpose and wired together.</li> <li>[ ] The plan is phased into approval-sized batches.</li> <li>[ ] Durable rules are separated out for <code>AGENTS.md</code>.</li> <li>[ ] No step assumes the builder can delete, deploy, or run SQL itself.</li> </ul> <p>Feeding the plan to the builder: paste the durable conventions into the project's <code>AGENTS.md</code> once; paste the data model + transactions into the agent chat phase by phase, approving each batch before moving on.</p>"},{"location":"reference/application-security/","title":"Application security reference","text":"<p>This guide describes authentication and authorization for one-shot generated SOAD applications. It is intended for application developers, deployers, and operators.</p>"},{"location":"reference/application-security/#architecture-and-production-boundary","title":"Architecture and production boundary","text":"<p>S0ADM is the online IDE and control plane. It is not an authentication service for deployed applications and must not be included in production images.</p> <p>Generated applications use one of these modes:</p> Mode Identity source When to use it <code>local</code> The application's own datasource and the framework auth runtime Protected applications; this is the default <code>custom</code> An application transaction implementing the provider interface SSO, OIDC, an existing account system, or another replacement <code>public</code> No identity provider Applications whose security profile is entirely public <p>In <code>local</code> mode the framework supplies login/logout, password reset, invitations, TOTP, profiles, session revocation, user administration, CSRF protection, and role/permission enforcement. Its routes are under <code>/_soad/auth/*</code>; it does not import or query <code>s0adm</code>.</p> <p>IDE sessions use keys such as <code>s0uid</code>. Application sessions use distinct <code>soad.auth.*</code> keys and a revocable record in the application database. An IDE login is therefore never accepted as an application login.</p>"},{"location":"reference/application-security/#generation-defaults","title":"Generation defaults","text":"<p>One-shot generation infers a security profile from the business requirements:</p> Application Profile and mode CRM, ERP, HR, invoicing, internal CRUD, dashboards, private/customer data <code>authenticated</code> + <code>local</code> Landing pages, documentation, brochure sites, genuinely public catalogs <code>public</code> + <code>public</code> Public content plus staff/customer workflows <code>mixed</code> + <code>local</code> Ambiguous brief <code>authenticated</code> + <code>local</code> <p>Operators can disable this inference with:</p> <pre><code>ai.one.shot.auto.security = false\n</code></pre> <p>The equivalent environment variable is <code>ai_one_shot_auto_security=false</code>. When disabled, one-shot generation uses public access without a login unless the product prompt explicitly requests authentication or access control. Explicit requests for login, private or protected access, accounts, roles, or permissions still produce the appropriate authenticated security plan. The default is <code>true</code>; restart the IDE after changing the property or environment variable.</p> <p>Local registration is <code>invite_only</code> unless the brief explicitly requires open registration. Open registration requires a least-privilege <code>member</code> role, which is granted to new accounts; it never grants <code>admin</code>. The generator does not create login, password, session, user, or ACL code inside business transactions.</p> <p>The accepted plan produces:</p> <ul> <li><code>WEB-INF/security/app-security.json</code>, the immutable runtime security manifest;</li> <li><code>WEB-INF/deploy/auth-mysql.sql</code>, the production installation bundle;</li> <li><code>WEB-INF/deploy/auth-h2.sql</code>, the development/test installation bundle.</li> </ul> <p>These artifacts come from the same normalized plan. The SQL contains schema, roles, and permissions but never users, password verifiers, invitations, sessions, or other secrets.</p>"},{"location":"reference/application-security/#manual-setup-without-one-shot-generation","title":"Manual setup (without one-shot generation)","text":"<p>An existing or hand-built app has no <code>app-security.json</code> until you configure one. Do this from the IDE, without the AI assistant:</p> <ol> <li>Open Settings \u2192 Application for the app.</li> <li>If the app has no security metadata yet, the Security card shows a    profile/mode picker instead of the read-only summary. Choose:</li> <li>Security profile \u2014 <code>public</code>, <code>authenticated</code>, or <code>mixed</code>.</li> <li>Authentication mode \u2014 <code>local</code> (framework-managed users in the app's      own database) or <code>custom</code> (your own provider transaction). <code>public</code> mode      is only offered when the profile is also <code>public</code>.</li> <li>Authenticated and mixed profiles require at least one role. Enter it in    the initial Roles editor:</li> </ol> <pre><code>admin|Administrator|Full access\nmember|Member\n</code></pre> <p>The example above expresses code, name, and description; enter these in the    corresponding role row fields. Only the role <code>code</code> is required (lowercase, starts with a letter, e.g.    <code>[a-z][a-z0-9_.:-]*</code>); <code>name</code> defaults to the code and <code>description</code> is    optional. Leave the box empty for a <code>public</code> profile \u2014 public profiles must    not declare roles. 4. Save. This validates the plan, writes the manifest and both SQL bundles,    and \u2014 for <code>local</code> mode \u2014 installs the local-auth schema into the app's    database immediately and grants your IDE account <code>system_admin</code> plus the    first listed role. A one-time, 20-minute setup link to set your local    password is shown on the page; do not share or log it.</p> <p>Initial setup creates roles without business permission grants. After saving, open the application's Roles and permissions section to register permission codes and assign grants to roles. The editor audits controller policies and identifies missing permission codes. Save to publish the updated manifest and install matching development metadata. Do not confuse this editor with the platform's IDE Roles screen. The transaction-editor AI path below is another way to stage a complete security plan.</p> <p>Switching <code>auth_mode</code> later (once a profile exists) uses the same Security card without re-entering roles \u2014 see Switching providers.</p>"},{"location":"reference/application-security/#alternative-the-per-transaction-editor-ai-superadmin-only","title":"Alternative: the per-transaction editor AI (superadmin only)","text":"<p>Besides the Settings form above and one-shot generation, a superadmin can also have the AI assistant propose a security plan while editing transactions in a normal (non-one-shot) chat session \u2014 including full roles and permissions:</p> <ul> <li>The chat session exposes a <code>stage_app_security</code> tool only to a superadmin.   Non-superadmins never see this tool; regular one-shot generation sessions   reject it too because one-shot generation uses its own embedded plan.</li> <li>Asking the assistant to set up or change roles/permissions/profile calls   <code>stage_app_security</code>, which builds and validates the manifest, checks that no already-assigned role   would be orphaned, and audits every routed transaction's <code>ACCESS_POLICY</code>   against the proposed plan \u2014 but only stages it. Nothing is written yet.</li> <li>The staged plan appears in the normal diff/approval UI alongside any staged   code changes. Approval re-checks that the approver is a superadmin, then uses   the same publication path as the manual form, so the   effect (manifest + SQL bundles written, local-auth schema installed, setup   link issued) is identical either way.</li> </ul> <p>Use this path when you want AI help drafting roles/permissions/rationale. The manual application Roles and permissions editor also supports those grants.</p>"},{"location":"reference/application-security/#controller-contract","title":"Controller contract","text":"<p>Each routed controller method needs an exact <code>ACCESS_POLICY</code> entry:</p> <pre><code>class Contact(object):\n    ACCESS_POLICY = {\n        \"view\": \"permission:contacts.read\",\n        \"save\": \"permission:contacts.write\",\n    }\n\n    def view(self, ctx):\n        current_user = ctx.output.get(\"current_user\")\n\n    def save(self, ctx):\n        \"\"\"POST\"\"\"\n        pass\n</code></pre> <p>Policy values are:</p> <ul> <li><code>public</code>;</li> <li><code>authenticated</code>;</li> <li><code>permission:&lt;manifest-permission-code&gt;</code>.</li> </ul> <p>Use the provider-neutral template values:</p> <pre><code>{{#if current_user}}\n  &lt;span&gt;{{current_user.full_name}}&lt;/span&gt;\n  &lt;a href=\"{{logout_url}}\"&gt;Sign out&lt;/a&gt;\n{{else}}\n  &lt;a href=\"{{login_url}}\"&gt;Sign in&lt;/a&gt;\n{{/if}}\n\n&lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{csrf_token}}\"&gt;\n</code></pre> <p><code>current_user</code> is in <code>ctx.output</code>; it is not an attribute of <code>ctx</code>. Never use <code>ctx.current_user</code> or <code>getattr(ctx, \"current_user\", None)</code>.</p>"},{"location":"reference/application-security/#deploying-local-authentication","title":"Deploying local authentication","text":"<p>Only MySQL production databases and H2 development/test databases are supported initially. Generation and installation reject other dialects.</p> <ol> <li> <p>Obtain the application-only runtime base and release/export workflow supplied    with your S\u00d8AD installation. The runtime base intentionally omits S0ADM.</p> </li> <li> <p>Export the generated application from the IDE, then build its application    layer using the supplied runtime base and release Dockerfile.</p> </li> <li> <p>Extract or obtain <code>WEB-INF/deploy/auth-mysql.sql</code> from the generated    workspace/image and apply it explicitly to the application database before    rollout:</p> </li> </ol> <pre><code>mysql --host DB_HOST --user DB_USER --password --database APP_DATABASE \\\n  &lt; webapp/WEB-INF/deploy/auth-mysql.sql\n</code></pre> <ol> <li>Configure the runtime datasource through environment variables:</li> </ol> <pre><code>db_default_url\ndb_default_driver_class\ndb_default_username\ndb_default_password\n</code></pre> <ol> <li>Create the first application administrator with password input on stdin:</li> </ol> <pre><code>printf '%s\\n' \"$INITIAL_PASSWORD\" |\n  docker exec -i APPLICATION_CONTAINER \\\n    soad-auth-admin create-admin \\\n    --app default \\\n    --email admin@example.com \\\n    --name \"Application Administrator\" \\\n    --password-stdin\n</code></pre> <p>Prefer a secret-file/stdin mechanism that does not place the password in    shell history. The CLI refuses password arguments, validates the manifest and    installed schema checksum, and grants both <code>system_admin</code> and the generated    owner/admin role.</p> <ol> <li>Check readiness:</li> </ol> <pre><code>GET /_soad/auth/health\n</code></pre> <p>It returns <code>200</code> only when the installed schema version and manifest checksum    match the image. A protected route returns <code>503</code> instead of running    unauthenticated when SQL has not been applied.</p> <p>The production configuration is <code>app/conf/sufia-runtime.properties</code>. It contains the application transaction and auth servlets only. It has no <code>db.s0adm</code>, <code>/s0adm/t/*</code>, IDE listener, IDE static mapping, extra Jython path, file editor, database editor, AI endpoint, deployment screen, or log servlet.</p>"},{"location":"reference/application-security/#development-setup","title":"Development setup","text":"<p>When one-shot generation publishes a protected application, the IDE explicitly installs the appropriate MySQL/H2 bundle into the development application database. It creates an application-local record matching the developer's email but does not copy the IDE password or trust the IDE session.</p> <p>The authenticated generation completion includes a 20-minute, single-use setup URL. Opening it sets a separate local password. Do not publish this URL in logs, source control, chat history, or deployment artifacts.</p>"},{"location":"reference/application-security/#built-in-pages-and-administration","title":"Built-in pages and administration","text":"<p>The local provider exposes:</p> Route Purpose <code>/_soad/auth/login</code>, <code>/logout</code> Session creation and revocation <code>/_soad/auth/bootstrap</code> Initial administrator setup when none is active <code>/_soad/auth/register</code> Open registration when enabled <code>/_soad/auth/forgot</code>, <code>/reset</code> Generic, single-use password recovery <code>/_soad/auth/invite</code> Invitation acceptance and initial password setup <code>/_soad/auth/profile</code>, <code>/password</code> Profile and password changes <code>/_soad/auth/totp/setup</code> Authenticator-app enrollment/removal <code>/_soad/auth/sessions</code> List and revoke active sessions <code>/_soad/auth/admin/users</code> Invite, enable/disable, and assign roles <code>/_soad/auth/health</code> Schema/manifest readiness <p><code>system_admin</code> includes the reserved <code>auth.users.manage</code> permission. Generated business roles are separate. Disabling a user or resetting a password revokes their active sessions.</p> <p>SMTP is optional for local development. When it is not configured, an administrator can copy the invitation link from the result page. Production password recovery requires mail configuration. Set <code>sufia_hostname</code> to the public HTTPS origin (for example, <code>https://app.example.com</code>) when the runtime is behind a reverse proxy so emailed links use the external address.</p>"},{"location":"reference/application-security/#customizing-built-in-authentication-pages","title":"Customizing built-in authentication pages","text":"<p>Local authentication supports validated Handlebars overrides under:</p> <pre><code>webapp/WEB-INF/auth-overrides/\n</code></pre> <p>The supported files are <code>login.html</code>, <code>verify-totp.html</code>, <code>register.html</code>, <code>bootstrap.html</code>, <code>forgot.html</code>, <code>reset.html</code>, <code>invite.html</code>, <code>profile.html</code>, <code>sessions.html</code>, <code>totp-setup.html</code>, <code>admin-users.html</code>, and <code>error.html</code>. A shared stylesheet may be placed at <code>webapp/assets/auth-overrides/auth.css</code>. Public mode has no login UI, and custom providers own their complete login flow.</p> <p>Every page receives escaped common values including:</p> <pre><code>{{title}}\n{{csrf_token}}\n{{message}}\n{{error}}\n{{context_path}}\n{{default_stylesheet_url}}\n{{stylesheet_url}}\n</code></pre> <p>Page contexts also provide their action/navigation URLs and applicable token, user, role, TOTP, or session-list data. Existing login placeholders such as <code>{{action_url}}</code> and <code>{{forgot_url}}</code> remain supported.</p> <p><code>admin-users</code> additionally provides <code>{{create_action_url}}</code> for the add-user form, whose fields are <code>full_name</code>, <code>email</code>, the optional <code>password</code> and <code>password_confirm</code>, and a repeated <code>role_codes</code> checkbox per entry in <code>{{roles}}</code>. Leaving both password fields empty creates the account pending and issues a one-time setup link, returned as <code>{{invite_link}}</code>; each entry in <code>{{users}}</code> carries a <code>pending</code> flag for that state. These inputs are optional in an override \u2014 a template that omits them keeps validating, and simply does not offer the feature.</p> <p>Overrides are limited to 128 KiB per HTML page and 256 KiB for the stylesheet. They must compile, retain the page's required form actions and input names, and render <code>{{csrf_token}}</code> for state-changing forms. Executable URLs, frames/objects, <code>&lt;base&gt;</code>, and unescaped triple-brace values are rejected on every screen. So is any URL pointing at another origin: an <code>https://</code>, <code>http://</code>, or <code>//host</code> value in <code>href</code>/<code>xlink:href</code>/<code>src</code>/<code>action</code>/<code>formaction</code>, or in an <code>@import</code>/<code>url()</code> in the stylesheet \u2014 no CDN stylesheet, remote image, font host, or off-site form target. Relative paths, <code>{{context_path}}/\u2026</code>, <code>#fragment</code>, <code>mailto:</code>, and <code>data:</code> URIs of image or font types are unaffected. A missing or invalid file logs a warning and falls back to the framework page.</p> <p>Script is allowed by screen, not globally:</p> <ul> <li>The four signed-in management screens -- <code>profile</code>, <code>sessions</code>, <code>totp-setup</code>,   <code>admin-users</code> -- accept an inline <code>&lt;script&gt;</code> and inline event handlers.   They are reached only after <code>_require_user</code> (and, for <code>admin-users</code>, the   <code>auth.users.manage</code> permission), so the reader is already authenticated, and   these list-and-row pages want a modal or a disclosure toggle to be usable.</li> <li>Every other screen is shown to an anonymous visitor and rejects both, because   script there can read the password field and the CSRF token.</li> <li>No screen accepts script from outside the application. <code>&lt;script src=...&gt;</code>   and <code>&lt;script type=\"module\"&gt;</code> (whose <code>import</code> can fetch remote code with no   <code>src</code>) are rejected everywhere: a fetched script can change under the   deployment, and it fails entirely in an air-gapped install.</li> </ul> <p>The four relaxed routes also carry a <code>Content-Security-Policy</code> response header (<code>script-src 'self' 'unsafe-inline'</code>, <code>connect-src 'self'</code>, <code>object-src 'none'</code>, <code>frame-ancestors 'none'</code>) so the browser enforces the same in/out distinction on what a regex over the markup cannot see -- a remote <code>import()</code> or <code>fetch()</code> from inside a script body. The sign-in flow sends no CSP.</p> <p>The transaction-editor AI stages these files through the normal diff and approval workflow. Deleting an override restores the framework default. An override changes presentation only; it never replaces credential verification, sessions, CSRF, roles, or permissions.</p> <p>For different authentication behavior rather than different styling, use a custom provider.</p>"},{"location":"reference/application-security/#changing-the-logo","title":"Changing the logo","text":"<p>The narrow, single-purpose screens (sign in, register, forgot, reset, invite, verify-2FA, error) show a small mark above the title. By default this is the framework's own SOAD icon; a developer can replace it by placing a bare, self-contained SVG at:</p> <pre><code>webapp/WEB-INF/auth-overrides/mark.svg\n</code></pre> <p>This is validated like the other overrides but is not a Handlebars template -- it is inlined as-is into the page, once per narrow screen, so it has its own, narrower rules:</p> <ul> <li>The whole file must be a single <code>&lt;svg&gt;...&lt;/svg&gt;</code> element: no XML   declaration, <code>&lt;!DOCTYPE&gt;</code>, or entity, and nothing before/after the root tag.</li> <li>No <code>&lt;script&gt;</code>, inline event handlers (<code>onload</code>, <code>onclick</code>, ...), frames,   <code>&lt;object&gt;</code>/<code>&lt;embed&gt;</code>, or <code>&lt;foreignObject&gt;</code> (which can smuggle arbitrary   HTML/script inside an SVG).</li> <li><code>href</code>/<code>xlink:href</code> may only point at a local <code>#fragment</code> (e.g. a <code>&lt;use&gt;</code>   referencing a <code>&lt;symbol&gt;</code> defined earlier in the same file) -- no external   file or <code>data:</code> URL, keeping the mark a single self-contained asset for   air-gapped deployments, same as everything else this stylesheet ships.</li> <li>Limited to 24 KiB.</li> </ul> <p>Any <code>class</code>/<code>aria-hidden</code> attribute on the root <code>&lt;svg&gt;</code> is stripped and replaced by the framework (<code>class=\"auth-mark\" aria-hidden=\"true\"</code>), so sizing comes from the page's own CSS regardless of what the file declares -- set a <code>viewBox</code> and skip <code>width</code>/<code>height</code> on the root element and let the page size it. A missing or invalid <code>mark.svg</code> falls back to the framework mark, same as any other override. The transaction-editor AI can stage it too, via <code>stage_auth_override</code> with <code>screen: \"mark\"</code>. Wide management screens (profile, sessions, 2FA, admin users) never show the mark, override or not.</p>"},{"location":"reference/application-security/#switching-providers","title":"Switching providers","text":"<p>In the IDE, open Settings \u2192 Application and choose:</p> <ul> <li>Application-local identity;</li> <li>Custom application provider;</li> <li>Public, available only for a public profile.</li> </ul> <p>Saving the setting regenerates the manifest and both SQL bundles. Controllers and their <code>ACCESS_POLICY</code> declarations do not need to change.</p>"},{"location":"reference/application-security/#custom-provider-interface","title":"Custom provider interface","text":"<p>Set the module to a lowercase application transaction path such as <code>security/auth_provider</code>. Its class must implement:</p> <pre><code>class Auth_provider(object):\n    def current_user(self, ctx):\n        return None\n\n    def authenticate(self, ctx):\n        return None\n\n    def authorize(self, ctx, action, permission):\n        return False\n\n    def validate_csrf(self, ctx):\n        return False\n\n    def login_url(self, ctx):\n        return \"%s/t/security/login\" % ctx.ctxPath\n\n    def logout_url(self, ctx):\n        return \"%s/t/security/login/logout\" % ctx.ctxPath\n</code></pre> <p>The provider transaction itself is not routable through the transaction engine. Methods fail closed: protected actions require a non-null authenticated user, permission checks require exactly <code>True</code>, and protected mutations require exactly <code>True</code> from <code>validate_csrf</code>.</p> <p>Switching to <code>custom</code> or <code>public</code> stops loading the built-in local provider. The reserved local tables may be left in place for an easy rollback or removed later through an operator-reviewed database migration; the runtime never drops them.</p>"},{"location":"reference/application-security/#security-behavior","title":"Security behavior","text":"<ul> <li>Passwords use PBKDF2-HMAC-SHA256, a random per-user salt, 600,000 iterations,   and stored algorithm/cost metadata.</li> <li>Login attempts are rate-limited and responses do not disclose whether an   account exists.</li> <li>Reset/invitation tokens are random, stored only as SHA-256 hashes, expire, and   are single-use.</li> <li>Protected local mutations require CSRF validation; public business mutations   must validate CSRF explicitly. Built-in auth forms validate their own tokens.</li> <li>Return URLs must be same-application absolute paths.</li> <li>Authentication rotates the HTTP session ID and CSRF token.</li> <li>Runtime sessions are revocable database records, not merely browser session   attributes.</li> <li>The shipped production profile uses <code>HttpOnly</code>, <code>SameSite=None</code>, and <code>Secure</code>.   The tracker deployment explicitly selects <code>Lax</code> for its same-site flow.</li> </ul> <p>The runtime image validation also fails when S0ADM is present or generated application source imports or links to it. In a valid production image, <code>/s0adm/*</code> has no mapping and returns <code>404</code>.</p>"},{"location":"reference/auth-overrides/","title":"Authentication override contract","text":"<p>Everything required to produce a drop-in replacement for the built-in authentication UI. A generator that follows this document can emit a folder that is copied into a deployed application and works without a restart.</p> <p>This document is structure only \u2014 it defines filenames, required markup, available data, and the validator rules. All visual design (layout, palette, typography, spacing, component styling) is left to the generator.</p> <p>The runtime validator and authentication context builder enforce this contract. See also application security.</p>"},{"location":"reference/auth-overrides/#1-output-layout","title":"1. Output layout","text":"<p>\u26a0\ufe0f Not every file goes into <code>WEB-INF/auth-overrides/</code>. The stylesheet must be web-reachable, so it lives under <code>webapp/assets/</code>. Emitting <code>auth.css</code> into <code>WEB-INF/auth-overrides/</code> results in a silently unstyled page \u2014 the file is never read and never served.</p> <p>Generate this tree, rooted at the application's <code>webapp/</code> directory:</p> <pre><code>webapp/\n\u251c\u2500\u2500 WEB-INF/\n\u2502   \u2514\u2500\u2500 auth-overrides/\n\u2502       \u251c\u2500\u2500 login.html\n\u2502       \u251c\u2500\u2500 verify-totp.html\n\u2502       \u251c\u2500\u2500 register.html\n\u2502       \u251c\u2500\u2500 bootstrap.html\n\u2502       \u251c\u2500\u2500 forgot.html\n\u2502       \u251c\u2500\u2500 reset.html\n\u2502       \u251c\u2500\u2500 invite.html\n\u2502       \u251c\u2500\u2500 profile.html\n\u2502       \u251c\u2500\u2500 sessions.html\n\u2502       \u251c\u2500\u2500 totp-setup.html\n\u2502       \u251c\u2500\u2500 admin-users.html\n\u2502       \u251c\u2500\u2500 error.html\n\u2502       \u2514\u2500\u2500 mark.svg          (optional logo; NOT a template \u2014 see \u00a78)\n\u2514\u2500\u2500 assets/\n    \u2514\u2500\u2500 auth-overrides/\n        \u2514\u2500\u2500 auth.css          (optional stylesheet \u2014 MUST be here, not WEB-INF)\n</code></pre> <p>Rules:</p> <ul> <li>Every file is independently optional. A screen with no override file falls   back to the framework page. A partial set is valid.</li> <li>No other filenames are recognized. Extra files in the folder are ignored \u2014   there is no way to add a page, a second stylesheet, an image, or a font file.   Everything a page needs must be in its own <code>.html</code> or in <code>auth.css</code>.</li> <li>Files are read per request, not cached. Adding, editing, or deleting a file   takes effect on the next page load. No restart, no rebuild. (Contrast with   ActiveJDBC models, which do require a restart.)</li> <li>UTF-8 only. Files are read as UTF-8; any other encoding makes the screen   fall back.</li> <li>Deleting a file restores the framework default.</li> <li>Overrides apply only when the application's auth mode is <code>local</code>. <code>public</code> mode   has no login UI; a <code>custom</code> provider owns its own flow.</li> </ul> <p>Size limits (exceeding = rejected):</p> File Limit any <code>.html</code> 128 KiB <code>auth.css</code> 256 KiB <code>mark.svg</code> 24 KiB"},{"location":"reference/auth-overrides/#2-failure-behavior-what-no-error-upon-run-means","title":"2. Failure behavior \u2014 what \"no error upon run\" means","text":"<p>An invalid override never produces a 500 or a broken page. The framework:</p> <ol> <li>calls the validator on every render (not just at install time),</li> <li>on any failure, logs <code>WARNING: Invalid authentication override &lt;screen&gt;</code>,</li> <li>renders its own built-in page instead.</li> </ol> <p>So the failure mode is silent reversion to the default look. If a generated page \"does nothing\", the file is being rejected \u2014 check the server log for the warning, which names the screen and the exact reason.</p> <p>Because validation runs per request, keep pages lean; multi-hundred-KiB pages pay a regex + Handlebars compile on every hit.</p>"},{"location":"reference/auth-overrides/#3-hard-rules-any-one-of-these-rejects-the-file","title":"3. Hard rules \u2014 any one of these rejects the file","text":"<p>The screens fall into two groups, and the script rules differ between them:</p> <ul> <li>Pre-sign-in screens \u2014 <code>login</code>, <code>verify-totp</code>, <code>register</code>, <code>bootstrap</code>,   <code>forgot</code>, <code>reset</code>, <code>invite</code>, <code>error</code>. Shown to an anonymous visitor, carrying   the password field and the CSRF token. No JavaScript of any kind.</li> <li>Signed-in management screens \u2014 <code>profile</code>, <code>sessions</code>, <code>totp-setup</code>,   <code>admin-users</code>. Reached only after authentication (<code>admin-users</code> additionally   requires the <code>auth.users.manage</code> permission). Inline JavaScript is allowed.</li> </ul> <p>Rejected on every screen:</p> Rejected Use instead <code>&lt;script src=\"\u2026\"&gt;</code> \u2014 any external script, incl. a CDN tag inline <code>&lt;script&gt;</code> on a management screen; <code>auth.css</code> for styling <code>&lt;script type=\"module\"&gt;</code> \u2014 its <code>import</code> can fetch remote code with no <code>src</code> a classic inline <code>&lt;script&gt;</code> <code>&lt;iframe&gt;</code>, <code>&lt;frame&gt;</code>, <code>&lt;frameset&gt;</code>, <code>&lt;object&gt;</code>, <code>&lt;embed&gt;</code>, <code>&lt;applet&gt;</code> plain HTML elements <code>href</code>/<code>xlink:href</code>/<code>src</code>/<code>action</code>/<code>formaction</code> starting <code>javascript:</code>, <code>vbscript:</code>, <code>data:text/html</code> a normal path The same attributes starting <code>https://</code>, <code>http://</code>, or <code>//host</code> \u2014 a CDN stylesheet, a remote image, a font host, an off-site form target, an outbound <code>&lt;a&gt;</code> a path on this application; embed an image or font as a <code>data:</code> URI <code>&lt;base&gt;</code> \u2014 it silently re-points every relative URL, including the form carrying the password nothing; the URLs you need are in the page context <code>&lt;meta http-equiv=\"refresh\"&gt;</code> let the server redirect <code>{{{value}}}</code> or <code>{{&amp;value}}</code> (unescaped output) <code>{{value}}</code> \u2014 always escaped <p>A <code>data:</code> URI of an image or font type is fine, and so are <code>mailto:</code> and <code>tel:</code> \u2014 none of them fetches from another origin. <code>{{stylesheet_url}}</code>, <code>{{context_path}}/\u2026</code>, <code>#fragment</code>, and any relative path are unaffected.</p> <p>Rejected on the pre-sign-in screens only:</p> Rejected Use instead <code>&lt;script&gt;</code> in any form, including inline <code>auth.css</code> \u2014 it is linked into every page Inline event handlers \u2014 any attribute matching <code>on\u2026=</code> (<code>onclick</code>, <code>onsubmit</code>, <code>onload</code>, \u2026) nothing; the forms are plain HTML POSTs and work without JS <p><code>auth.css</code>: no <code>expression(</code>, <code>javascript:</code>, <code>vbscript:</code>, <code>url(data:text/html\u2026)</code>, and no <code>@import</code> or <code>url()</code> pointing at another origin.</p> <p>Three implementation details that trip generators:</p> <ul> <li>The check runs on HTML-unescaped text. Writing <code>&amp;lt;script&amp;gt;</code> as visible   page text still trips the script rule. Do not put these constructs in prose.</li> <li>On a pre-sign-in screen the event-handler pattern is <code>on&lt;word&gt;=</code>, matched   anywhere, including in text content. A sentence such as <code>run once = twice</code>   matches and rejects the file. Avoid a word beginning with \"on\" followed   directly by <code>=</code>. (This trap does not apply to the four management screens.)</li> <li>Attribute values must be quoted. The required-input check matches   <code>name=\"email\"</code> or <code>name='email'</code>; unquoted <code>name=email</code> fails.</li> </ul> <p>On the pre-sign-in screens, no JavaScript at all is the single biggest constraint. Every interaction must be a plain form submit or a link. Client-side validation, password-strength meters, show/hide-password toggles, and AJAX are impossible there. CSS-only interaction (<code>:focus-within</code>, <code>:checked</code>, <code>:has()</code>) is permitted and is the supported way to get interactive behavior.</p> <p>On the four management screens, write the script inline in the page \u2014 a modal, a disclosure panel, a confirm-before-revoke, a client-side filter over the user list are all fine. Those routes also send a <code>Content-Security-Policy</code> (<code>script-src 'self' 'unsafe-inline'</code>, <code>connect-src 'self'</code>, <code>object-src 'none'</code>, <code>frame-ancestors 'none'</code>, <code>img-src 'self' data:</code>), so a remote <code>import()</code> or a <code>fetch()</code> to a third-party origin is blocked by the browser even though no regex inspects the script body. Keep the page working if the script does not run: these are the screens a user manages their own credentials from.</p>"},{"location":"reference/auth-overrides/#4-page-shell-required-and-easy-to-get-wrong","title":"4. Page shell \u2014 required, and easy to get wrong","text":"<p>An HTML override replaces the entire document. The framework's layout is not applied around it. Consequences the generator must handle:</p> <ul> <li>It must emit a complete document: <code>&lt;!doctype html&gt;</code>, <code>&lt;html&gt;</code>, <code>&lt;head&gt;</code>,   <code>&lt;body&gt;</code>. Nothing is added.</li> <li>No stylesheet is linked for you. Emit the links yourself.</li> <li><code>mark.svg</code> is not injected into an overridden page. The logo override only   applies to screens still using the framework page. An override that wants a   logo must inline its own SVG (subject to \u00a73's rules) or draw it in CSS.</li> <li><code>{{message}}</code> and <code>{{error}}</code> are not rendered for you. If the page does   not output them, the user never sees \"Invitation accepted\", \"Too many   attempts\", or \"Passwords do not match\". Render both on every screen.</li> </ul> <p>Two stylesheet URLs are in the context of every screen:</p> Variable Points at Use <code>{{default_stylesheet_url}}</code> the framework's <code>auth.css</code> link it first to inherit framework styling, or omit it entirely to start from a blank slate <code>{{stylesheet_url}}</code> your <code>webapp/assets/auth-overrides/auth.css</code> link it last <p>Required shell (structure only \u2014 design is the generator's):</p> <pre><code>&lt;!doctype html&gt;\n&lt;html lang=\"en\"&gt;\n&lt;head&gt;\n&lt;meta charset=\"utf-8\"&gt;\n&lt;meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"&gt;\n&lt;meta name=\"robots\" content=\"noindex,nofollow\"&gt;\n&lt;title&gt;{{title}}&lt;/title&gt;\n&lt;link rel=\"stylesheet\" href=\"{{stylesheet_url}}\"&gt;\n&lt;/head&gt;\n&lt;body&gt;\n  {{#if error}}&lt;div class=\"notice error\"&gt;{{error}}&lt;/div&gt;{{/if}}\n  {{#if message}}&lt;div class=\"notice success\"&gt;{{message}}&lt;/div&gt;{{/if}}\n  &lt;!-- screen body, per \u00a76 --&gt;\n&lt;/body&gt;\n&lt;/html&gt;\n</code></pre> <p><code>noindex,nofollow</code> is on the framework pages and should be kept.</p> <p>If <code>auth.css</code> is not generated, drop the <code>{{stylesheet_url}}</code> link \u2014 otherwise every page load logs a 404 for a file that does not exist.</p>"},{"location":"reference/auth-overrides/#5-template-language-the-available-subset","title":"5. Template language \u2014 the available subset","text":"<p>Handlebars (Java, jknack). Available helpers and nothing else:</p> <ul> <li>Built-ins: <code>if</code>, <code>unless</code>, <code>each</code>, <code>with</code>, <code>lookup</code>, <code>log</code></li> <li>Comparison: <code>eq</code>, <code>neq</code>, <code>gt</code>, <code>gte</code>, <code>lt</code>, <code>lte</code>, <code>and</code>, <code>or</code>, <code>not</code></li> </ul> <p>The app-view helpers from <code>server/utils/render.py</code> \u2014 <code>select</code>, <code>option</code>, <code>ref_lookup</code>, <code>dateFmt</code>, <code>in</code>, <code>html</code>, <code>session</code>, <code>i18n</code>, <code>get</code> \u2014 are deliberately unavailable here. Using one is a compile failure, and the screen falls back.</p> <pre><code>{{#if registration_open}}&lt;a href=\"{{register_url}}\"&gt;Create an account&lt;/a&gt;{{/if}}\n{{#each users}}&lt;li&gt;{{full_name}} \u2014 {{email}}&lt;/li&gt;{{/each}}\n{{#if (eq status \"active\")}}\u2026{{/if}}\n</code></pre> <p>All output is HTML-escaped. There is no way to emit raw HTML from a value.</p>"},{"location":"reference/auth-overrides/#6-per-screen-contract","title":"6. Per-screen contract","text":""},{"location":"reference/auth-overrides/#common-context-present-on-every-screen","title":"Common context \u2014 present on every screen","text":"Variable Type Notes <code>title</code> string the framework's title for the screen <code>message</code> string success notice; <code>\"\"</code> when none <code>error</code> string error notice; <code>\"\"</code> when none <code>csrf_token</code> string see below <code>context_path</code> string servlet context path; <code>\"\"</code> at root <code>default_stylesheet_url</code> string framework <code>auth.css</code> <code>stylesheet_url</code> string your <code>auth.css</code>"},{"location":"reference/auth-overrides/#the-two-structural-requirements","title":"The two structural requirements","text":"<p>For every screen that has inputs (all except <code>error</code>):</p> <ol> <li>A <code>&lt;form&gt;</code> with an <code>action</code> attribute must appear in the file. Matched as    <code>&lt;form \u2026 action=\u2026&gt;</code> within one tag.</li> <li>Every listed input name must appear as <code>name=\"\u2026\"</code>, and the literal    <code>{{csrf_token}}</code> placeholder must be present. A hardcoded hidden field does    not satisfy this \u2014 the placeholder is required precisely so that CSRF    protection cannot be disabled by a template.</li> </ol> <p>Standard CSRF field for every form:</p> <pre><code>&lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{csrf_token}}\"&gt;\n</code></pre> <p>The check is a regex over the raw file, so names inside <code>{{#if}}</code> / <code>{{#each}}</code> blocks count as present even if that branch never renders. A screen with two mutually exclusive forms (<code>totp-setup</code>) satisfies the check by containing both.</p>"},{"location":"reference/auth-overrides/#loginhtml-post-action_url","title":"<code>login.html</code> \u2014 POST <code>{{action_url}}</code>","text":"<p>Required names: <code>email</code>, <code>password</code>, <code>csrf_token</code></p> Variable Type Notes <code>action_url</code> string POST target <code>forgot_url</code> string password recovery <code>register_url</code> string only meaningful when <code>registration_open</code> <code>registration_open</code> bool show/hide the register link <code>bootstrap_available</code> bool true when no administrator exists yet; link to <code>{{context_path}}/_soad/auth/bootstrap</code> <pre><code>&lt;form method=\"post\" action=\"{{action_url}}\"&gt;\n  &lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{csrf_token}}\"&gt;\n  &lt;input type=\"email\" name=\"email\" required&gt;\n  &lt;input type=\"password\" name=\"password\" required&gt;\n  &lt;button type=\"submit\"&gt;Sign in&lt;/button&gt;\n&lt;/form&gt;\n&lt;a href=\"{{forgot_url}}\"&gt;Forgot password?&lt;/a&gt;\n{{#if registration_open}}&lt;a href=\"{{register_url}}\"&gt;Create an account&lt;/a&gt;{{/if}}\n{{#if bootstrap_available}}\n  &lt;a href=\"{{context_path}}/_soad/auth/bootstrap\"&gt;Set up the first administrator&lt;/a&gt;\n{{/if}}\n</code></pre>"},{"location":"reference/auth-overrides/#verify-totphtml-post-action_url","title":"<code>verify-totp.html</code> \u2014 POST <code>{{action_url}}</code>","text":"<p>Shown after a correct password when TOTP is enabled. Credentials are already held in the session; the form carries only the code.</p> <p>Required names: <code>code</code>, <code>csrf_token</code> Context: <code>action_url</code></p>"},{"location":"reference/auth-overrides/#registerhtml-post-action_url","title":"<code>register.html</code> \u2014 POST <code>{{action_url}}</code>","text":"<p>Only reachable when open registration is enabled.</p> <p>Required names: <code>full_name</code>, <code>email</code>, <code>password</code>, <code>password_confirm</code>, <code>csrf_token</code> Context: <code>action_url</code>, <code>login_url</code></p>"},{"location":"reference/auth-overrides/#bootstraphtml-post-action_url","title":"<code>bootstrap.html</code> \u2014 POST <code>{{action_url}}</code>","text":"<p>First-administrator setup; reachable only while no active administrator exists.</p> <p>Required names: <code>full_name</code>, <code>email</code>, <code>password</code>, <code>password_confirm</code>, <code>csrf_token</code> Context: <code>action_url</code>, <code>login_url</code>, <code>password_min_length</code> (integer, currently 10)</p> <p>Use <code>minlength=\"{{password_min_length}}\"</code> on both password inputs.</p>"},{"location":"reference/auth-overrides/#forgothtml-post-action_url","title":"<code>forgot.html</code> \u2014 POST <code>{{action_url}}</code>","text":"<p>One template serves both the empty form and the post-submit confirmation; branch on <code>submitted</code>.</p> <p>Required names: <code>email</code>, <code>csrf_token</code> Context: <code>action_url</code>, <code>login_url</code>, <code>submitted</code> (bool)</p> <p>The response is deliberately generic \u2014 it does not reveal whether the account exists. Do not word the confirmation as though it does.</p>"},{"location":"reference/auth-overrides/#resethtml-post-action_url","title":"<code>reset.html</code> \u2014 POST <code>{{action_url}}</code>","text":"<p>Required names: <code>token</code>, <code>password</code>, <code>password_confirm</code>, <code>csrf_token</code> Context: <code>action_url</code>, <code>token</code>, <code>login_url</code>, <code>password_min_length</code></p> <p><code>token</code> must be carried through as a hidden field:</p> <pre><code>&lt;input type=\"hidden\" name=\"token\" value=\"{{token}}\"&gt;\n</code></pre>"},{"location":"reference/auth-overrides/#invitehtml-post-action_url","title":"<code>invite.html</code> \u2014 POST <code>{{action_url}}</code>","text":"<p>Invitation acceptance / initial password. Identical shape to <code>reset.html</code>; a separate file so the wording can differ.</p> <p>Required names: <code>token</code>, <code>password</code>, <code>password_confirm</code>, <code>csrf_token</code> Context: <code>action_url</code>, <code>token</code>, <code>login_url</code>, <code>password_min_length</code></p> <p>The four screens that follow \u2014 <code>profile</code>, <code>sessions</code>, <code>totp-setup</code>, <code>admin-users</code> \u2014 are the signed-in management screens. They are the only ones that accept an inline <code>&lt;script&gt;</code> and inline event handlers (see \u00a73), and the only ones that get the wide card. <code>error</code>, after them, is back to the pre-sign-in rules.</p> <p>Shared shell context \u2014 on all four, so the same nav, sign-out link, and \"signed in as\" can be pasted into each:</p> Variable Type Notes <code>profile_url</code>, <code>sessions_url</code>, <code>totp_setup_url</code>, <code>admin_users_url</code> string navigation <code>logout_url</code> string plain link; the CSRF token is already in the query string, so a template cannot build this itself <code>user</code> object <code>id</code>, <code>email</code>, <code>full_name</code>, <code>status</code> <p><code>{{admin_users_url}}</code> is present for every user; non-administrators get 403 on that page. Hide it or accept the 403 \u2014 there is no \"is admin\" flag here.</p>"},{"location":"reference/auth-overrides/#profilehtml-two-forms","title":"<code>profile.html</code> \u2014 two forms","text":"<p>Required names: <code>full_name</code>, <code>current_password</code>, <code>password</code>, <code>password_confirm</code>, <code>csrf_token</code></p> Variable Type Notes <code>action_url</code> string POST \u2014 profile update (<code>full_name</code>) <code>password_action_url</code> string POST \u2014 password change <p>Two separate forms are required \u2014 one to <code>{{action_url}}</code> with <code>full_name</code>, one to <code>{{password_action_url}}</code> with <code>current_password</code> + <code>password</code> + <code>password_confirm</code>. Each needs its own CSRF field.</p>"},{"location":"reference/auth-overrides/#sessionshtml-post-revoke_action_url-per-row","title":"<code>sessions.html</code> \u2014 POST <code>{{revoke_action_url}}</code> per row","text":"<p>Required names: <code>session_id</code>, <code>csrf_token</code></p> Variable Type Notes <code>revoke_action_url</code> string POST target <code>sessions</code> array see below <p>Each entry of <code>sessions</code>: <code>id</code>, <code>created_at</code>, <code>last_seen_at</code>, <code>user_agent</code>, <code>ip_address</code> \u2014 all strings, pre-defaulted (<code>\"Unknown client\"</code>, <code>\"unknown address\"</code>), newest first.</p> <p>Each row needs its own form; there is no bulk revoke endpoint.</p> <pre><code>{{#each sessions}}\n  &lt;li&gt;\n    {{user_agent}} \u2014 {{ip_address}} \u2014 {{last_seen_at}}\n    &lt;form method=\"post\" action=\"{{../revoke_action_url}}\"&gt;\n      &lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{../csrf_token}}\"&gt;\n      &lt;input type=\"hidden\" name=\"session_id\" value=\"{{id}}\"&gt;\n      &lt;button type=\"submit\"&gt;Revoke&lt;/button&gt;\n    &lt;/form&gt;\n  &lt;/li&gt;\n{{/each}}\n</code></pre> <p>Note the <code>../</code> prefix inside <code>{{#each}}</code>. The validator's <code>{{csrf_token}}</code> check is satisfied by the shell's plain <code>{{csrf_token}}</code>, but each form still needs the <code>../</code>-qualified value to actually render \u2014 so include at least one bare <code>{{csrf_token}}</code> outside the loop if every form is inside one.</p>"},{"location":"reference/auth-overrides/#totp-setuphtml-two-mutually-exclusive-states","title":"<code>totp-setup.html</code> \u2014 two mutually exclusive states","text":"<p>Required names: <code>code</code>, <code>current_password</code>, <code>csrf_token</code> \u2014 all three must be in the file even though only one state renders at a time.</p> Variable Type Notes <code>enabled</code> bool branch on this <code>secret</code> string base32 secret; <code>\"\"</code> when already enabled <code>qr_data_url</code> string <code>data:image/\u2026</code> for <code>&lt;img src=\"\u2026\"&gt;</code>; <code>\"\"</code> when enabled <code>setup_action_url</code> string POST \u2014 enable (field <code>code</code>) <code>disable_action_url</code> string POST \u2014 disable (field <code>current_password</code>) <p><code>{{qr_data_url}}</code> in an <code>src</code> attribute is allowed \u2014 only <code>data:text/html</code> is blocked, image data URLs are fine. Show <code>{{secret}}</code> as text too, for users who cannot scan.</p> <pre><code>{{#if enabled}}\n  &lt;form method=\"post\" action=\"{{disable_action_url}}\"&gt;\n    &lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{csrf_token}}\"&gt;\n    &lt;input type=\"password\" name=\"current_password\" required&gt;\n    &lt;button type=\"submit\"&gt;Disable&lt;/button&gt;\n  &lt;/form&gt;\n{{else}}\n  &lt;img alt=\"TOTP QR code\" src=\"{{qr_data_url}}\"&gt;\n  &lt;code&gt;{{secret}}&lt;/code&gt;\n  &lt;form method=\"post\" action=\"{{setup_action_url}}\"&gt;\n    &lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{csrf_token}}\"&gt;\n    &lt;input name=\"code\" inputmode=\"numeric\" required&gt;\n    &lt;button type=\"submit\"&gt;Enable&lt;/button&gt;\n  &lt;/form&gt;\n{{/if}}\n</code></pre>"},{"location":"reference/auth-overrides/#admin-usershtml-four-post-targets","title":"<code>admin-users.html</code> \u2014 four POST targets","text":"<p>The most demanding screen. Required names: <code>email</code>, <code>user_id</code>, <code>role_code</code>, <code>operation</code>, <code>status</code>, <code>csrf_token</code> \u2014 all six, even for a minimal page.</p> Variable Type Notes <code>create_action_url</code> string POST \u2014 add user <code>invite_action_url</code> string POST \u2014 (re-)send setup link; field <code>email</code> <code>role_action_url</code> string POST \u2014 grant/remove role <code>status_action_url</code> string POST \u2014 enable/disable <code>users</code> array see below <code>roles</code> array <code>{ code, name }</code> <code>form</code> object re-populates the add-user form after a validation error: <code>full_name</code>, <code>email</code>, <code>role_codes</code> (array) <code>invite_link</code> string one-time setup link, <code>\"\"</code> when none <code>invitation_created</code> bool true only on the invite-result render, where <code>users</code> and <code>roles</code> are empty <p>Each entry of <code>users</code>: <code>id</code>, <code>email</code>, <code>full_name</code>, <code>status</code>, <code>pending</code> (bool), <code>roles</code> (array of role-code strings), <code>next_status</code> (the value to post to flip enabled/disabled).</p> <p><code>pending</code> means the account exists but has no password \u2014 it cannot sign in until the setup link is followed. Render it distinctly from <code>active</code>.</p> <p>The four operations:</p> <pre><code>{{! 1. add user \u2014 role_codes is a repeated checkbox, name is PLURAL here }}\n&lt;form method=\"post\" action=\"{{create_action_url}}\"&gt;\n  &lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{csrf_token}}\"&gt;\n  &lt;input name=\"full_name\" value=\"{{form.full_name}}\" required&gt;\n  &lt;input type=\"email\" name=\"email\" value=\"{{form.email}}\" required&gt;\n  {{#each roles}}\n    &lt;label&gt;&lt;input type=\"checkbox\" name=\"role_codes\" value=\"{{code}}\"&gt; {{name}}&lt;/label&gt;\n  {{/each}}\n  &lt;input type=\"password\" name=\"password\" minlength=\"10\"&gt;\n  &lt;input type=\"password\" name=\"password_confirm\" minlength=\"10\"&gt;\n  &lt;button type=\"submit\"&gt;Create user&lt;/button&gt;\n&lt;/form&gt;\n{{! leaving both password fields empty creates a pending account + setup link }}\n\n{{#if invite_link}}&lt;p&gt;&lt;a href=\"{{invite_link}}\"&gt;{{invite_link}}&lt;/a&gt;&lt;/p&gt;{{/if}}\n\n{{#each users}}\n  &lt;li&gt;\n    {{full_name}} \u2014 {{email}} \u2014 {{#if pending}}pending{{else}}{{status}}{{/if}}\n    {{#each roles}}&lt;span&gt;{{this}}&lt;/span&gt;{{/each}}\n\n    {{! 2. grant/remove a role \u2014 role_code is SINGULAR here }}\n    &lt;form method=\"post\" action=\"{{../role_action_url}}\"&gt;\n      &lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{../csrf_token}}\"&gt;\n      &lt;input type=\"hidden\" name=\"user_id\" value=\"{{id}}\"&gt;\n      &lt;input type=\"hidden\" name=\"operation\" value=\"grant\"&gt;\n      &lt;select name=\"role_code\"&gt;\n        {{#each ../roles}}&lt;option value=\"{{code}}\"&gt;{{name}}&lt;/option&gt;{{/each}}\n      &lt;/select&gt;\n      &lt;button type=\"submit\"&gt;Grant&lt;/button&gt;\n    &lt;/form&gt;\n\n    {{! 3. enable/disable }}\n    &lt;form method=\"post\" action=\"{{../status_action_url}}\"&gt;\n      &lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{../csrf_token}}\"&gt;\n      &lt;input type=\"hidden\" name=\"user_id\" value=\"{{id}}\"&gt;\n      &lt;input type=\"hidden\" name=\"status\" value=\"{{next_status}}\"&gt;\n      &lt;button type=\"submit\"&gt;{{#if (eq status \"active\")}}Disable{{else}}Enable{{/if}}&lt;/button&gt;\n    &lt;/form&gt;\n\n    {{! 4. re-send setup link \u2014 only useful for pending accounts }}\n    {{#if pending}}\n    &lt;form method=\"post\" action=\"{{../invite_action_url}}\"&gt;\n      &lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{../csrf_token}}\"&gt;\n      &lt;input type=\"hidden\" name=\"email\" value=\"{{email}}\"&gt;\n      &lt;button type=\"submit\"&gt;Re-send setup link&lt;/button&gt;\n    &lt;/form&gt;\n    {{/if}}\n  &lt;/li&gt;\n{{/each}}\n</code></pre> <p>Watch the two role field names: <code>role_codes</code> (plural, checkboxes, create form) and <code>role_code</code> (singular, select, grant/remove). Only the singular one is required by the validator; the plural create-form fields are optional \u2014 a template that omits them keeps validating and simply does not offer that feature. To remove a role, post <code>operation</code> = <code>remove</code> instead of <code>grant</code>.</p> <p><code>{{invite_link}}</code> must be shown when non-empty: with SMTP unconfigured it is the only way the administrator can deliver the setup link.</p>"},{"location":"reference/auth-overrides/#errorhtml-no-form","title":"<code>error.html</code> \u2014 no form","text":"<p>Used for every failure response: 400, 403 (CSRF mismatch, non-admin), 404 (disabled feature, unknown page), 500, 503 (schema/manifest not ready).</p> <p>Required names: none. No form or CSRF needed.</p> Variable Type Notes <code>status</code> integer HTTP status <code>error</code> string the message; also in <code>{{error}}</code> <p>The HTTP status code is preserved regardless of what the template renders. Keep the message generic \u2014 it is shown to unauthenticated visitors.</p>"},{"location":"reference/auth-overrides/#7-authcss","title":"7. <code>auth.css</code>","text":"<p>Path: <code>webapp/assets/auth-overrides/auth.css</code>, served at <code>{{stylesheet_url}}</code> (= <code>&lt;context&gt;/assets/auth-overrides/auth.css</code>).</p> <ul> <li>Rejected: <code>expression(</code>, <code>javascript:</code>, <code>vbscript:</code>, <code>url(data:text/html\u2026)</code>,   and any <code>@import</code> or <code>url()</code> pointing at another origin (<code>https://</code>,   <code>http://</code>, <code>//host</code>).</li> <li>Limit 256 KiB.</li> <li>Self-contained. There is no second file, no image, no font file \u2014 the   folder recognizes only the names in \u00a71. Embed any image or font as a <code>data:</code>   URI (image/font types are fine; only <code>data:text/html</code> is blocked). Do not   <code>@import</code> or reference a CDN: these pages are built for air-gapped   deployments, and a fetch that fails leaves the login screen unstyled.</li> <li>If the generator emits full-page HTML overrides with self-contained class   names, it does not need to link <code>{{default_stylesheet_url}}</code>, and the   framework class names below are irrelevant.</li> <li>If the generator overrides only some screens, the non-overridden ones still   render with the framework layout, which links the framework stylesheet and   then yours. In that case <code>auth.css</code> is a cascade layer over the defaults, and   these are the framework's hooks:</li> </ul> <p><code>.auth-card</code> / <code>.auth-card.wide</code>, <code>.auth-mark</code>, <code>.auth-body</code>, <code>.notice</code>,   <code>.notice.error</code>, <code>.notice.success</code>, <code>nav.tab-bar</code>, <code>.row-main</code>,   <code>.row-identity</code>, <code>.row-sub</code>, <code>.row-roles</code>, <code>.row-actions</code>, <code>.badge</code>,   <code>.badge-active</code>, <code>.badge-disabled</code>, <code>.badge-pending</code>, <code>.chip</code>, <code>.chip-remove</code>,   <code>.inline</code>, <code>.form-hint</code>, <code>.link-box</code>, <code>.role-picker</code>, <code>.role-option</code>.</p> <p><code>.wide</code> is applied to <code>profile</code>, <code>sessions</code>, <code>totp-setup</code>, and <code>admin-users</code>;   the other screens are the narrow centered card.</p> <p>Recommended: for a consistent result, generate either all 12 HTML files, or none of them plus <code>auth.css</code> only. A partial HTML set means overridden and framework screens sit side by side in the same flow.</p>"},{"location":"reference/auth-overrides/#8-marksvg-optional-logo","title":"8. <code>mark.svg</code> (optional logo)","text":"<p>Path: <code>webapp/WEB-INF/auth-overrides/mark.svg</code>.</p> <p>This only affects screens rendered by the framework layout. An overridden HTML page does not receive it \u2014 inline the logo in that page's own markup instead. It is worth generating only when shipping <code>auth.css</code> without HTML overrides.</p> <p>Not a Handlebars template \u2014 inlined verbatim. Its rules:</p> <ul> <li>The whole file is one <code>&lt;svg&gt;\u2026&lt;/svg&gt;</code> element. No <code>&lt;?xml?&gt;</code>, no <code>&lt;!DOCTYPE&gt;</code>,   no <code>&lt;!ENTITY&gt;</code>, nothing before or after the root tag.</li> <li>No <code>&lt;script&gt;</code>, no <code>on\u2026=</code> handlers, no <code>&lt;iframe&gt;</code>/<code>&lt;object&gt;</code>/<code>&lt;embed&gt;</code>/   <code>&lt;foreignObject&gt;</code>.</li> <li><code>href</code>/<code>xlink:href</code> may only be a local <code>#fragment</code> (e.g. <code>&lt;use&gt;</code> referencing a   <code>&lt;symbol&gt;</code> in the same file). No external file, no <code>data:</code> URL.</li> <li>Build from shape elements: <code>&lt;svg&gt;</code>, <code>&lt;g&gt;</code>, <code>&lt;path&gt;</code>, <code>&lt;circle&gt;</code>, <code>&lt;ellipse&gt;</code>,   <code>&lt;rect&gt;</code>, <code>&lt;polygon&gt;</code>, <code>&lt;use&gt;</code>.</li> <li>24 KiB limit.</li> <li>Set a <code>viewBox</code>; omit <code>width</code>/<code>height</code>. Any <code>class</code> or <code>aria-hidden</code> on the   root is stripped and replaced with <code>class=\"auth-mark\" aria-hidden=\"true\"</code>, so   sizing comes from CSS.</li> <li>Shown on the narrow screens only (login, register, forgot, reset, invite,   verify-2FA, error). Wide management screens never show it.</li> </ul>"},{"location":"reference/auth-overrides/#9-generator-checklist","title":"9. Generator checklist","text":"<p>Per HTML file:</p> <ul> <li>[ ] Complete document: doctype, <code>&lt;html&gt;</code>, <code>&lt;head&gt;</code>, <code>&lt;body&gt;</code></li> <li>[ ] <code>&lt;title&gt;{{title}}&lt;/title&gt;</code> and the <code>{{stylesheet_url}}</code> link (only if       <code>auth.css</code> is generated)</li> <li>[ ] Both <code>{{message}}</code> and <code>{{error}}</code> rendered</li> <li>[ ] Every required input name from \u00a76, quoted: <code>name=\"email\"</code></li> <li>[ ] A literal <code>{{csrf_token}}</code> placeholder, plus a CSRF hidden field in every       form (<code>../csrf_token</code> inside <code>{{#each}}</code>)</li> <li>[ ] Every form has an <code>action</code> attribute pointing at the screen's <code>*_action_url</code></li> <li>[ ] No <code>{{{\u2026}}}</code>, and no <code>&lt;script src=\u2026&gt;</code> or <code>&lt;script type=\"module\"&gt;</code> anywhere</li> <li>[ ] On a pre-sign-in screen: no <code>&lt;script&gt;</code> at all and no <code>on\u2026=</code> attribute       (check prose text too). On <code>profile</code> / <code>sessions</code> / <code>totp-setup</code> /       <code>admin-users</code>: inline script and handlers are fine</li> <li>[ ] Only the \u00a75 helpers used</li> <li>[ ] Under 128 KiB, UTF-8</li> </ul> <p>Per output set:</p> <ul> <li>[ ] <code>auth.css</code> at <code>webapp/assets/auth-overrides/</code>, not <code>WEB-INF/</code></li> <li>[ ] No files other than the names in \u00a71</li> <li>[ ] No external URL anywhere \u2014 no CDN, font host, or remote image. This is       enforced, not advisory: an <code>https://</code>, <code>http://</code>, or <code>//host</code> URL in       <code>href</code>/<code>src</code>/<code>action</code>/<code>formaction</code>, or in a CSS <code>@import</code>/<code>url()</code>,       rejects the file</li> </ul> <p>Verification after copying in: load each route below and confirm the page is yours, not the framework's. A framework-looking page means the file was rejected \u2014 the server log names the screen and the reason.</p> <pre><code>GET  /_soad/auth/login          \u2192 login.html\nPOST /_soad/auth/login          \u2192 verify-totp.html (TOTP users)\nGET  /_soad/auth/register       \u2192 register.html      (open registration only)\nGET  /_soad/auth/bootstrap      \u2192 bootstrap.html     (no admin yet)\nGET  /_soad/auth/forgot         \u2192 forgot.html\nGET  /_soad/auth/reset?token=\u2026  \u2192 reset.html\nGET  /_soad/auth/invite?token=\u2026 \u2192 invite.html\nGET  /_soad/auth/profile        \u2192 profile.html\nGET  /_soad/auth/sessions       \u2192 sessions.html\nGET  /_soad/auth/totp/setup     \u2192 totp-setup.html\nGET  /_soad/auth/admin/users    \u2192 admin-users.html\nGET  /_soad/auth/nonexistent    \u2192 error.html (404)\nGET  /_soad/auth/health         \u2192 readiness JSON (not overridable)\n</code></pre>"},{"location":"reference/auth-overrides/#10-scope","title":"10. Scope","text":"<p>An override changes presentation only. It cannot change credential verification, session handling, CSRF, roles, permissions, rate limiting, or token lifetimes, and it cannot add or remove a route. For different authentication behavior, use a custom provider instead.</p>"},{"location":"reference/configuration/","title":"Configuration reference","text":"<p>Properties are loaded from <code>SUFIA_HOME/conf/sufia.properties</code>; environment variables overlay them. The normal mapping replaces underscores with dots: <code>db_default_driver_class</code> becomes <code>db.default.driver.class</code>. Use the documented spelling rather than inventing an alias.</p> <p>The development profile is <code>app/conf/sufia.properties</code>; application-only images copy <code>app/conf/sufia-runtime.properties</code> into that runtime location. Do not copy environment-specific values from the development profile into docs or releases. Restart for configuration changes unless a feature explicitly documents live updates.</p>"},{"location":"reference/configuration/#application-and-database","title":"Application and database","text":"Property Environment Meaning sufia.home SUFIA_HOME Runtime root; special startup variable sufia.mode sufia_mode development reloads transaction modules; production for deployments sufia.context_path sufia_context_path Servlet context; special compatibility handling sufia.app.name sufia_app_name Application display name for runtime pages/email sufia.hostname sufia_hostname Public origin used for generated links sufia.welcome.transaction sufia_welcome_transaction Operator override for root welcome transaction db.default.url db_default_url JDBC URL for default app db.default.driver.class db_default_driver_class JDBC driver, e.g. com.mysql.cj.jdbc.Driver db.default.username db_default_username Database account db.default.password db_default_password Database password, injected as a secret <p>The driver reader also accepts legacy <code>driverclass</code>/<code>driver_class</code> spellings, but examples use <code>driver.class</code>. Each configured datasource name is also an application name. Do not assume the single-app local-auth manifest architecture provides arbitrary multi-tenant security automatically.</p> <p>Welcome resolution is the operator property first, then the released <code>WEB-INF/sufia-app.properties</code>, then the framework fallback. An explicit blank operator value selects the existing static index instead of a transaction.</p>"},{"location":"reference/configuration/#sessions-and-uploads","title":"Sessions and uploads","text":"Property Shipped production value / behavior session.cookie.httponly true session.cookie.secure true; use HTTPS session.cookie.samesite NONE; tracker examples override to LAX session.timeout 1800 seconds servlet idle timeout max.file.upload.size Maximum individual upload bytes; legacy maxfileuploadsize takes precedence if set max.request.upload.size Aggregate request bytes; legacy maxrequestuploadsize takes precedence file.upload.temp.dir Upload spool directory; otherwise JVM tmp/soad-uploads <p>Environment forms include <code>session_cookie_secure</code>, <code>session_cookie_samesite</code>, <code>session_timeout</code>, and <code>max_file_upload_size</code>. Check whether a legacy upload property is already set before adding a dotted replacement. Application auth records have their own expiry and revocation checks. Jetty's file session store uses JVM tmp/<code>s0-session-store</code>.</p>"},{"location":"reference/configuration/#email-and-ai","title":"Email and AI","text":"<p>SMTP: <code>mail.smtp.host</code>, <code>mail.smtp.port</code>, <code>mail.smtp.username</code>, <code>mail.smtp.password</code>, <code>mail.sender.email</code>. The corresponding variables replace dots with underscores. Read the current TLS limitation in the integration chapter.</p> <p>AI: <code>ai.enabled</code>, <code>ai.provider</code>, <code>ai.endpoint</code>, <code>ai.api.key</code>, <code>ai.model</code>, <code>ai.auto.accept</code>. IDE-saved fields take precedence over properties/environment. <code>ai.one.shot.auto.security</code> defaults true; turning it off changes inference, not explicit requests for protected access.</p> <p>Generation limits include <code>ai.turn.cap</code>, <code>ai.one.shot.turn.cap</code>, <code>ai.context.max.tokens</code>, and <code>ai.max.output.tokens</code>; tune them against the selected provider's actual limits. <code>ai.turn.cap</code> is the maximum number of model/tool-use round trips for a normal IDE chat task. <code>ai.one.shot.turn.cap</code> is the separate, usually higher limit for generating a complete dashboard or application from an empty workspace. A new transaction commonly takes about three turns, so raise the relevant cap only when the task needs more work; keeping it modest prevents a weak model from repeating unproductive tool calls.</p> <p><code>ai.context.max.tokens</code> is the history budget sent on each round; older whole turns are trimmed while the full transcript remains stored. It must leave room inside the model's context window for the system prompt, tool results, and the new response. Smaller-context or less capable models generally need a lower turn cap and a conservative context budget. For a model with a 1M-token window, a larger budget such as <code>200000</code> can preserve more of a long build's history, but it is still deliberately below the advertised window. The environment names are <code>ai_turn_cap</code>, <code>ai_one_shot_turn_cap</code>, and <code>ai_context_max_tokens</code>. These are development/IDE settings, not runtime application requirements.</p>"},{"location":"reference/configuration/#advanced-runtime-mappings","title":"Advanced runtime mappings","text":"<p>Servlet/filter mappings are configured as <code>servlet.&lt;name&gt;.mapping/file</code> and <code>filter.&lt;name&gt;.mapping/file</code>. Ordinary application transactions need no per-route configuration. Static mappings use <code>static.&lt;name&gt;.mapping/dir</code>. Context listeners use the configured listener mechanism.</p> <p>Error overrides use <code>error.page.&lt;status&gt;</code> and <code>error.page_exception</code>; see error pages. The development-only manifest exemption setting <code>sufia.security.manifest.exempt_apps</code> exists for the IDE; do not exempt your protected application to silence a setup error.</p>"},{"location":"reference/error-pages/","title":"Error page reference","text":"<p>The runtime bundles three generic HTML error pages. They use <code>/assets/soad-auth/auth.css</code>, include a context-aware home link, and never display exception messages or stack traces. Responses use UTF-8 and <code>Cache-Control: no-store</code>.</p> Error Transaction override Class Conventional template 404 (including missing resources and transactions) <code>default/errors/err404.py</code> <code>Err404</code> <code>errors/_err404/err404.html</code> Explicit <code>sendError(500)</code> <code>default/errors/err500.py</code> <code>Err500</code> <code>errors/_err500/err500.html</code> Uncaught exception (HTTP 500) <code>default/errors/errexc.py</code> <code>Errexc</code> <code>errors/_errexc/errexc.html</code> <p>Paths are relative to <code>WEB-INF/jython/</code>; template paths are relative to its <code>default/</code> directory. Include <code>__init__.py</code> in <code>default/</code> and <code>default/errors/</code> when creating the files manually. Each override is optional and discovered independently. Default-app overrides handle application requests, including other application names. IDE (<code>/s0adm</code>, <code>/assets/s0adm</code>), internal <code>/_soad</code> and <code>/logs</code> errors always use bundled pages.</p>"},{"location":"reference/error-pages/#conventional-views","title":"Conventional views","text":"<p>For example, create <code>default/errors/err404.py</code>:</p> <pre><code>class Err404:\n    def view(self, ctx):\n        ctx.output['heading'] = 'This page is no longer available'\n        ctx.output['home_url'] = ctx.getCtxPath() + '/'\n</code></pre> <p>Create <code>default/errors/_err404/err404.html</code>:</p> <pre><code>&lt;!doctype html&gt;\n&lt;html lang=\"en\"&gt;\n&lt;head&gt;\n  &lt;meta charset=\"utf-8\"&gt;\n  &lt;meta name=\"viewport\" content=\"width=device-width, initial-scale=1\"&gt;\n  &lt;title&gt;Page not found&lt;/title&gt;\n  &lt;link rel=\"stylesheet\" href=\"{{ctxPath}}/assets/soad-auth/auth.css\"&gt;\n&lt;/head&gt;\n&lt;body&gt;\n  &lt;main class=\"auth-card auth-body\"&gt;\n    &lt;h1&gt;{{heading}}&lt;/h1&gt;\n    &lt;p&gt;Please return home to continue.&lt;/p&gt;\n    &lt;a class=\"auth-button\" href=\"{{home_url}}\"&gt;Go home&lt;/a&gt;\n  &lt;/main&gt;\n&lt;/body&gt;\n&lt;/html&gt;\n</code></pre> <p>Only <code>view(ctx)</code> runs: no automatic database connection or transaction, authentication check, application filter, routed action, or page-layout hook. <code>ctx.output</code> and <code>ctxPath</code> work as usual. Any database access explicitly added by an override is its responsibility and may fail during an outage.</p>"},{"location":"reference/error-pages/#callable-rendering","title":"Callable rendering","text":"<p>An explicit 500 override can reuse a named template. Create <code>default/errors/err500.py</code>:</p> <pre><code>from utils import render\n\nclass Err500:\n    def view(self, ctx):\n        ctx.output['heading'] = 'Please try again later'\n        ctx.output['home_url'] = ctx.getCtxPath() + '/'\n        ctx.go_to = render.as_view(ctx, 'err404', group='errors', code='err404')\n</code></pre> <p>This uses the complete template above. Any callable <code>ctx.go_to</code> is supported; its returned text is written to the buffered response. It may instead write to <code>ctx.getResponse()</code> and return <code>None</code>. A renderer setting status 200 does not change the original 404 or 500 status. Output is published as UTF-8 HTML; override headers, cookies and content types are not published.</p> <p>Redirects (including string <code>ctx.go_to</code>) and secondary <code>sendError</code> calls fail the override. Output, including flushed output, is buffered and discarded on failure. The failure is logged and the matching bundled page is used. If the bundled page also fails, the runtime emits minimal generic HTML. Recursive dispatch is rejected. A response already committed before error handling cannot be replaced.</p>"},{"location":"reference/error-pages/#explicit-disclosure-of-error-details","title":"Explicit disclosure of error details","text":"<p><code>ctx.getError()</code> returns <code>None</code> outside the dedicated error dispatch. During it, it returns the captured original error map:</p> <ul> <li><code>status</code>: original HTTP status (500 for exceptions).</li> <li><code>kind</code>: <code>err404</code>, <code>err500</code>, or <code>errexc</code>.</li> <li><code>message</code>: servlet error message, possibly <code>None</code>.</li> <li><code>request_uri</code>: original request URI, including the context path.</li> <li><code>exception</code>: Java/Jython exception object, or <code>None</code>.</li> <li><code>stack_trace</code>: preserved runtime Jython traceback followed by the Java stack   when an exception exists; an empty string for responses without an exception.</li> </ul> <p>The map is not automatically included in template data. To opt in, create <code>default/errors/errexc.py</code>:</p> <pre><code>from webgeaz.sufia.core import Application\n\n\ndef should_show_details(ctx):\n    # Example policy: deliberately opt in only on a private development server.\n    return (Application.getProperty('sufia.mode') == 'development'\n            and Application.getProperty('errors.show_details') == 'true')\n\n\nclass Errexc:\n    def view(self, ctx):\n        error = ctx.getError()\n        ctx.output['show_stack'] = should_show_details(ctx)\n        if ctx.output['show_stack']:\n            ctx.output['stack_trace'] = error['stack_trace']\n</code></pre> <p>Create <code>default/errors/_errexc/errexc.html</code>:</p> <pre><code>&lt;!doctype html&gt;\n&lt;html lang=\"en\"&gt;\n&lt;head&gt;&lt;meta charset=\"utf-8\"&gt;&lt;title&gt;Request failed&lt;/title&gt;&lt;/head&gt;\n&lt;body&gt;\n  &lt;h1&gt;Something went wrong&lt;/h1&gt;\n  &lt;a href=\"{{ctxPath}}/\"&gt;Go home&lt;/a&gt;\n  {{#if show_stack}}&lt;pre&gt;{{stack_trace}}&lt;/pre&gt;{{/if}}\n&lt;/body&gt;\n&lt;/html&gt;\n</code></pre> <p>Use escaped <code>{{stack_trace}}</code>, never triple braces. Exception messages may contain user-controlled text. The disclosure policy belongs entirely to override code; the runtime never enables it automatically, even in development.</p>"},{"location":"reference/error-pages/#configuration-and-rollout","title":"Configuration and rollout","text":"<p>Existing <code>error.page.&lt;status&gt;</code> settings replace the corresponding status mapping. <code>error.page_exception</code> replaces the uncaught-exception mapping. Exception routing is separate from explicit 500 routing; configure <code>error.page_exception</code> to replace the exception page. Explicit configured pages retain their existing servlet lifecycle; the dedicated lifecycle described above applies to the built-in route.</p> <p>Publish these runtime files through the container release workflow supplied with your S\u00d8AD installation, then restart the runtime. Restart after changing error mappings or production override code. In <code>sufia.mode=development</code>, overrides reload on each error request, following transaction conventions. Bundled HTML is read on each error. No migration is needed. Existing PDF rendering error handling is unchanged.</p>"},{"location":"reference/migrations/","title":"Database migrations and older installations","text":"<p>There are two separate database lifecycles: S0ADM's control-plane schema and your application's business/auth schema. Choose the intended database before running any SQL.</p>"},{"location":"reference/migrations/#existing-s0adm-installations","title":"Existing S0ADM installations","text":"<p>New IDE databases are initialized by the S0ADM container when empty. An existing database is not rebuilt by restarting an image. IDE control-plane migrations are provided with the S\u00d8AD distribution and must be applied by the deployment operator against the installed version.</p> File Feature 001_ai_chat.sql AI conversations 002_user_2fa.sql IDE second-factor fields 003_user_avatar.sql Profile avatar 004_app_security.sql Application security metadata 005_custom_auth_provider.sql Provider selection 006_ai_provider_settings.sql IDE AI provider settings 007_security_rationale_length.sql Expanded rationale 008_ai_auto_accept.sql AI auto-accept setting 009_user_ui_theme.sql User theme preference <p>Back up before changes. Stop the S0ADM container before applying an embedded-H2 migration through the operator procedure supplied with the image; this avoids file-lock ambiguity. Use only the migration applicable to the installed release and database target. For MySQL use your normal reviewed migration client.</p> <p>Existing installs need the schema update and refreshed models, not just a newer container image.</p>"},{"location":"reference/migrations/#application-migrations","title":"Application migrations","text":"<p>Keep ordered, version-controlled business SQL in your application project. The tracker initial schema is for a fresh database; evolve it using new migrations rather than editing and replaying the initial script.</p> <p>Local-auth generation produces <code>auth-mysql.sql</code> and <code>auth-h2.sql</code> beside the security manifest. Install the bundle for your dialect. The manifest checksum must match installed metadata; changing roles/permissions requires compatible metadata deployment, not just controller edits.</p> <p>Auth bundles contain schema/role/permission metadata, not users or secrets. Review grant changes because reinstalling metadata can change effective access. Back up user/session data separately.</p>"},{"location":"reference/migrations/#deployment-order","title":"Deployment order","text":"<ol> <li>Back up and confirm the target database.</li> <li>Apply reviewed auth/business migrations.</li> <li>Start the matching image so models regenerate.</li> <li>Check auth health and application behavior.</li> </ol> <p>Rollback must pair a compatible image, manifest, and database state. MySQL DDL may commit implicitly; a failed migration is not necessarily automatically undone.</p>"},{"location":"reference/rendering/","title":"Rendering and utility reference","text":"<p>Import <code>from utils import render</code>. Most rendering functions return a callable: assign it to <code>ctx.go_to</code>. Set <code>ctx.page_layout=False</code> for non-page responses.</p>"},{"location":"reference/rendering/#renderers","title":"Renderers","text":"Call Result / important parameters as_view(ctx, view, group=None, code=None, loc=None, file=None, combine_map=None) Handlebars HTML; view name omits .html; sets status 200 as_string_template(ctx, view) Inline Handlebars string rendered from output as_json(ctx, obj=None, status=200) JSON; omitted obj serializes all output as_html(ctx, code=None, loc=None) Reads raw HTML without template expansion as_string(ctx, text_string, status=200) Literal HTML string, not escaped text as_file(ctx, file, content_type, filename=None, attachment=True) Streams a filesystem path or java.io.File as_blob(ctx, content, content_type, filename, attachment=False) Streams byte content as_pdf(ctx, view, group=None, code=None, file=None, filename=None, combine_map=None, attachment=False) Converts a Handlebars document to PDF <p>Examples:</p> <pre><code>ctx.go_to = render.as_view(ctx, \"edit\")\nctx.go_to = render.as_view(ctx, \"summary\", group=\"reports\", code=\"monthly\")\nctx.go_to = render.as_json(ctx, {\"ok\": True}, status=201)\nctx.go_to = render.as_blob(ctx, data, \"application/octet-stream\",\n                          \"report.bin\", attachment=True)\n</code></pre> <p>These lines are alternatives; only the last assigned response is used. Some JDBC drivers return a <code>java.sql.Blob</code> rather than bytes. For deliberately bounded blobs, call <code>blob.getBytes(1, int(blob.length()))</code> before <code>as_blob</code>; its input must support byte-array operations. Stream large values separately. Avoid <code>loc</code>/<code>file</code> unless intentionally resolving a framework-specific alternate view location. Never let a submitted path select an arbitrary template/download. <code>as_string</code> is HTML, so use constant text or explicit escaping for user values.</p>"},{"location":"reference/rendering/#template-helpers","title":"Template helpers","text":"Helper Example / purpose eq, neq, gt, gte, lt, lte <code>{{#eq status \"done\"}}Complete{{/eq}}</code> and, or, not Conditional composition length, size <code>{{length projects}}</code> for collections dateFmt <code>{{dateFmt created_date \"yyyy-MM-dd\"}}</code> html Escape text, preserve newlines, linkify URLs select, option Build select markup/options from supplied refs or a model table ref_lookup Resolve a display label by model table or supplied refs session Read a named session attribute; do not expose secrets get Resolve an output key as safe content; use only trusted values i18n Resolve resource-bundle labels <p>The normal view renderer also registers Handlebars string helpers. Model-backed select/ref helpers can perform database queries. Prefer prefiltered <code>refs</code> for private data so the helper cannot accidentally show a global table:</p> <pre><code>{{select name=\"assignee_id\" refs=people label=\"full_name\" value=\"id\" selected=task.assignee_id}}\n</code></pre> <p>Do not put untrusted SQL in a helper's <code>filter</code> attribute. For reusable views, prepare lookup lists in the controller to avoid per-row queries.</p>"},{"location":"reference/rendering/#email-and-java-libraries","title":"Email and Java libraries","text":"<p>The SMTP entrypoint is:</p> <pre><code>from utils import mailer\nmailer.send(None, \"recipient@example.com\", \"Project update\",\n            \"Your project was updated.\", html=False)\n</code></pre> <p>Its full signature is <code>send(fr, to, subject, content, cc=[], bcc=[], html=False, attachment=[], inline=[], reply_to=None)</code>. Use actual configured recipients and reviewed templates. <code>send_api</code> is a separate provider-specific path; inspect its configured API integration before adopting it. See email deployment constraints.</p> <p>Bundled libraries include Java time/IO, Google Guava, Apache Commons, Apache POI, Jakarta Mail, and the PDF converter. Check the library list for your installed S\u00d8AD version; do not infer that an arbitrary Java JAR is bundled.</p> <p>Log through <code>from sufia.util import Log</code> and <code>Log.info(ctx, message)</code>. Use <code>utils.fileupload.UploadedFile</code> for multipart file handling rather than calling a second upload parser.</p>"},{"location":"reference/transaction-filters/","title":"Transaction filter reference","text":"<p>Transaction filters let an application run reusable request and response logic around the normal servlet pipeline. They are useful for concerns such as request logging, response headers, maintenance gates, correlation IDs, and other logic that must run before or after a request reaches a servlet.</p> <p>S\u00d8AD implements this feature with one framework filter mapped to <code>/*</code>. The framework filter discovers transaction classes in the reserved <code>filter</code> group and presents each one as a servlet-style filter.</p>"},{"location":"reference/transaction-filters/#quick-start","title":"Quick start","text":"<p>Create a transaction named <code>filter/request_logger</code>:</p> <pre><code>webapp/WEB-INF/jython/default/\n\u251c\u2500\u2500 __init__.py\n\u2514\u2500\u2500 filter/\n    \u251c\u2500\u2500 __init__.py\n    \u2514\u2500\u2500 request_logger.py\n</code></pre> <p>Define all three lifecycle methods in <code>request_logger.py</code>:</p> <pre><code>from java.util.logging import Level, Logger\n\n\nlogger = Logger.getLogger(\"filter.request_logger\")\n\n\nclass Request_logger(object):\n    url_mapping = \"/*\"\n\n    def filter_init(self, config):\n        logger.log(Level.INFO, \"Request logger initialized\")\n\n    def do_filter(self, request, response, chain):\n        logger.log(Level.INFO, \"Request: %s\" % request.getRequestURI())\n        chain.doFilter(request, response)\n\n    def destroy(self):\n        logger.log(Level.INFO, \"Request logger destroyed\")\n</code></pre> <p>Restart or redeploy the server after creating, deleting, renaming, or changing a transaction filter. Discovery and transaction instances are cached for the lifetime of the framework filter.</p>"},{"location":"reference/transaction-filters/#required-contract","title":"Required contract","text":"<p>A Python file qualifies as a transaction filter when all of the following are true:</p> <ul> <li>It is directly under <code>webapp/WEB-INF/jython/default/filter/</code>.</li> <li>Its filename is a valid transaction code, for example <code>request_logger.py</code>.</li> <li>It declares the conventional transaction class. The filename   <code>request_logger.py</code> maps to the class <code>Request_logger</code>.</li> <li>The class implements <code>filter_init</code>, <code>do_filter</code>, and <code>destroy</code> with the   signatures described below.</li> </ul> <pre><code>class Example(object):\n    url_mapping = \"/*\"\n\n    def filter_init(self, config):\n        pass\n\n    def do_filter(self, request, response, chain):\n        chain.doFilter(request, response)\n\n    def destroy(self):\n        pass\n</code></pre> <p>The <code>filter</code> group is reserved for this contract. A filter transaction is not a normal MVC endpoint: do not add <code>view(ctx)</code>, a Handlebars view directory, or an <code>ACCESS_POLICY</code> merely to make it look like a routed transaction.</p>"},{"location":"reference/transaction-filters/#filter_initconfig","title":"<code>filter_init(config)</code>","text":"<p>The framework calls <code>filter_init</code> once after constructing the filter. <code>config</code> is the <code>jakarta.servlet.FilterConfig</code> of the framework dispatcher and provides access to the servlet context and configured initialization parameters.</p> <p>Use this method for immutable setup that can be shared safely across requests. If it raises an exception, filter initialization fails and the error is allowed to propagate to the servlet container.</p>"},{"location":"reference/transaction-filters/#do_filterrequest-response-chain","title":"<code>do_filter(request, response, chain)</code>","text":"<p>The framework calls <code>do_filter</code> for every request that matches the transaction's URL mapping. The arguments are the current servlet request, servlet response, and continuation chain.</p> <p>Call <code>chain.doFilter(request, response)</code> exactly once to continue to the next matching transaction filter and eventually the requested servlet. Code before that call is pre-processing; code after it is post-processing:</p> <pre><code>class Timing(object):\n    def filter_init(self, config):\n        pass\n\n    def do_filter(self, request, response, chain):\n        started = System.nanoTime()\n        try:\n            chain.doFilter(request, response)\n        finally:\n            elapsed = System.nanoTime() - started\n            # Record elapsed here.\n\n    def destroy(self):\n        pass\n</code></pre> <p>Import <code>System</code> from <code>java.lang</code> when using the timing example.</p> <p>A filter may intentionally omit the chain call to short-circuit the request. When it does, it must complete the response itself:</p> <pre><code>class Maintenance(object):\n    url_mapping = \"/t/admin/*\"\n\n    def filter_init(self, config):\n        pass\n\n    def do_filter(self, request, response, chain):\n        if request.getHeader(\"X-Maintenance-Bypass\") is None:\n            response.sendError(503, \"Service temporarily unavailable\")\n            return\n        chain.doFilter(request, response)\n\n    def destroy(self):\n        pass\n</code></pre> <p>Do not catch and suppress downstream errors unless the filter has a documented recovery policy. An exception raised by a transaction filter, a later filter, or the target servlet propagates with normal servlet-filter behavior.</p>"},{"location":"reference/transaction-filters/#destroy","title":"<code>destroy()</code>","text":"<p>The framework calls <code>destroy</code> during shutdown or filter replacement. Release resources created by <code>filter_init</code> here. Filters are destroyed in reverse initialization order. Destruction failures are not silently converted into successful shutdowns.</p>"},{"location":"reference/transaction-filters/#url-mappings","title":"URL mappings","text":"<p>Declare an optional class-level <code>url_mapping</code>. It may be one string or a list of strings. When it is absent, the mapping defaults to <code>/*</code>.</p> <pre><code>class Api_headers(object):\n    url_mapping = [\"/t/api/*\", \"*.json\"]\n\n    def filter_init(self, config):\n        pass\n\n    def do_filter(self, request, response, chain):\n        response.setHeader(\"X-Content-Type-Options\", \"nosniff\")\n        chain.doFilter(request, response)\n\n    def destroy(self):\n        pass\n</code></pre> <p>Mappings follow servlet URL-pattern conventions:</p> Pattern Matches <code>/*</code> Every request <code>/health</code> Only the exact <code>/health</code> path <code>/t/api/*</code> <code>/t/api</code> and paths below it <code>*.json</code> Any request path ending in <code>.json</code> <p>Patterns are matched against the request path relative to the servlet context. The query string is not part of the match. A list matches when any one of its patterns matches. An empty value, unsupported pattern, or non-string list item is a configuration error and fails initialization.</p> <p>The transaction mapping decides whether that transaction is invoked; it does not register another container filter. The single framework dispatcher remains mapped to <code>/*</code>.</p>"},{"location":"reference/transaction-filters/#discovery-order-and-application-scope","title":"Discovery, order, and application scope","text":"<p>The dispatcher scans only the <code>default/filter</code> package. These filters are deployment-wide and may therefore see requests for named applications, built-in authentication routes, static resources, and other servlets. Always narrow <code>url_mapping</code> when a filter should affect only part of the server.</p> <p>Eligible filters are initialized and invoked in ascending alphabetical order by transaction code. For deliberate ordering, use descriptive prefixes that still follow transaction naming rules:</p> <pre><code>filter/a_request_id.py\nfilter/b_request_log.py\nfilter/c_response_headers.py\n</code></pre> <p>Only filters whose mapping matches the current request enter that request's chain. Post-processing runs in the reverse order naturally as nested chain calls return.</p> <p>Discovery happens during dispatcher initialization and is cached. A server restart or redeployment is required after adding, removing, or renaming a filter and after changing filter code or mappings.</p>"},{"location":"reference/transaction-filters/#failure-handling","title":"Failure handling","text":"<p>The <code>filter</code> group is tolerant of incomplete transaction files but strict once a filter qualifies:</p> Condition Behavior One or more required lifecycle methods are missing Skip the transaction and log a warning Module import or class construction fails Fail initialization and propagate the error <code>url_mapping</code> is invalid Fail initialization and propagate the error <code>filter_init</code> fails Fail initialization and propagate the error <code>do_filter</code> or downstream processing fails Propagate the request error <code>destroy</code> fails Report the destruction error <p>Skipping an incomplete class supports ordinary files being inspected while they are being created, but it can also hide an intended security filter. Check the server log after deployment and test security-sensitive filters explicitly.</p>"},{"location":"reference/transaction-filters/#runtime-and-concurrency-rules","title":"Runtime and concurrency rules","text":"<p>Transaction filter instances follow servlet-filter singleton semantics. One cached instance may serve many requests concurrently.</p> <ul> <li>Do not store request-specific data on <code>self</code>.</li> <li>Prefer local variables and request attributes for per-request state.</li> <li>Make shared mutable state thread-safe.</li> <li>Keep <code>filter_init</code> setup immutable where possible.</li> <li>Do not assume a <code>WebContext</code>, ActiveJDBC connection, or database transaction.</li> <li>Do not assume the request is a <code>/t/...</code> transaction request.</li> <li>Do not write a response after it has been committed by downstream processing.</li> </ul> <p>The arguments to <code>do_filter</code> are raw servlet objects. If a filter needs a value later in the request pipeline, attach it to the request:</p> <pre><code>request.setAttribute(\"correlation_id\", correlation_id)\n</code></pre>"},{"location":"reference/transaction-filters/#default-framework-registration","title":"Default framework registration","text":"<p>The framework dispatcher is enabled in both development and production runtime profiles with:</p> <pre><code>filter.transaction_filter.mapping = /*\nfilter.transaction_filter.file = server/TransactionFilter.py\n</code></pre> <p>Applications normally do not need to add these properties themselves. If a custom runtime configuration replaces the supplied <code>sufia.properties</code>, retain both entries to keep transaction-filter support enabled.</p>"},{"location":"reference/transaction-filters/#ai-agent-generation-rules","title":"AI agent generation rules","text":"<p>When an AI agent creates or edits a transaction in the <code>filter</code> group, it must apply the following rules:</p> <ol> <li>Treat <code>filter</code> as the reserved deployment-wide transaction-filter group.</li> <li>Generate one controller file and no view templates unless the developer    explicitly requests unrelated files.</li> <li>Generate the conventional capitalized class with all three methods:    <code>filter_init(config)</code>, <code>do_filter(request, response, chain)</code>, and <code>destroy()</code>.</li> <li>Do not generate <code>view(ctx)</code> or <code>ACCESS_POLICY</code> for the filter contract.</li> <li>Declare <code>url_mapping</code> only when needed; otherwise rely on the <code>/*</code> default.</li> <li>Call <code>chain.doFilter(request, response)</code> exactly once for normal continuation,    or intentionally complete the response and return for a short circuit.</li> <li>Preserve servlet exception behavior; do not add a blanket exception handler    that hides failures.</li> <li>Keep the instance thread-safe and do not place request state on <code>self</code>.</li> <li>Tell the developer that a restart or redeployment is required for the change    to be discovered.</li> </ol> <p>Before proposing a filter, the agent should state its transaction code, URL mapping, whether it continues or short-circuits the chain, and any shared state created during initialization.</p>"},{"location":"reference/transaction-filters/#review-checklist","title":"Review checklist","text":"<ul> <li>The file is under <code>default/filter</code> and its class name matches its filename.</li> <li>All three lifecycle methods use the exact required signatures.</li> <li>The mapping is absent, a valid string, or a valid list of strings.</li> <li>The chain is called exactly once on every continuation path.</li> <li>Every short-circuit path completes the response.</li> <li>Exceptions are allowed to propagate unless recovery is intentional.</li> <li>Shared state is safe for concurrent requests.</li> <li>The mapping is narrow enough for deployment-wide scope.</li> <li>Deployment includes a server restart or redeployment.</li> </ul>"},{"location":"reference/troubleshooting/","title":"Troubleshooting and glossary","text":""},{"location":"reference/troubleshooting/#diagnosis-table","title":"Diagnosis table","text":"Symptom Explanation / next check Model import fails Table missing, wrong datasource, or models not refreshed Attribute/method missing in Jython Check Python 2.7 syntax and actual Java method signature Saving does nothing Assign ctx.go_to; inspect errors; manual string IDs need insert on first save Login loop App account vs IDE account; HTTPS and Secure/SameSite cookie settings 403 for every user Missing ACCESS_POLICY, unregistered permission, no role grants, or CSRF 503 on protected route Manifest missing/invalid or installed auth checksum differs Unexpected 404 Wrong class capitalization, route, failed import, or ownership intentionally hidden 413 on upload Per-file or aggregate request limit JSON appears inside HTML Disable page_layout for that endpoint Styles missing after deployment Public assets absent, wrong ctxPath, or dependence on IDE-only assets Auth customization ignored Validator rejection, wrong directory, missing form/context requirements New filter is not active Restart; filter discovery is cached AI feature missing Master switch, provider settings, IDE privileges/image/migrations AI changes applied without a click Check auto-accept and whole-app generation mode Release build unavailable Kaniko missing from the development image Auth health green but page fails Health checks auth metadata, not all business tables Docs diagram shown as text Mermaid support/JavaScript blocked; inspect hosted site and CSP <p>Avoid logging secrets while investigating. Reproduce one request, inspect its actual status, body, redirects, and relevant log excerpt, then verify database state if it was a mutation.</p>"},{"location":"reference/troubleshooting/#glossary","title":"Glossary","text":"Term Meaning Transaction A group/code Jython controller and its views Action A routed public method, normally view/save/edit Database transaction Commit/rollback boundary around request work WebContext / ctx Request/response bridge and template output map Model Generated ActiveJDBC class for a database table/view Introspect Regenerate model metadata/classes from database schema S0ADM Browser IDE and development control plane Runtime Application-serving process/container Security profile Public, authenticated, or mixed application classification Auth mode Local, custom, or public identity implementation ACCESS_POLICY Controller mapping from action names to access requirements Permission Registered capability granted through roles Ownership Business rule restricting which records a user may access Manifest Versioned application security metadata with checksum Release Source/image artifact; not a database backup Skill Instructions/reference bundle loaded by a compatible coding agent"}]}